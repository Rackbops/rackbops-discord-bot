# warbandeer-discord — Code Context

> **Purpose:** Discord bot (Bun + TypeScript, discord.js v14) — a generic, modular core that installs
> feature plugins from a published manifest. The core provides GitHub release announcements for watched
> repos, self-update, and a `/report` command that files GitHub issues from Discord; the World of
> Warcraft features (`/dmf`, `/reset`, `/status`, `/transmog`, and the Darkmoon Faire / weekly-reset /
> realm-watch announcements) are the `@rackbops/plugin-wow` plugin (#107), and character linking is
> `@rackbops/plugin-warbandeer` (#100).
>
> **Fork.** Extracted with full history from [nazumods/wow](https://github.com/nazumods/wow)'s
> `apps/warbandeer-discord` — designed and built there by
> [Nazuraki](https://github.com/nazumods), who gets full credit for the original bot and
> every gotcha documented below. This repo is now the bot's own root (no monorepo, no addon
> release pipeline); self-update was repointed at this fork accordingly — see the **Fork
> gotchas** entry below for what's still open.

## Language

**Link Code**:
A short-lived (~10 min), single-use code minted by `/link` and entered into the Warbandeer
desktop app to authorize a Device Token. Not itself a credential for pushing character data —
only for redeeming one, once.
_Avoid_: linking code, auth code, pairing code

**Device Token**:
The long-lived bearer credential the desktop app receives by redeeming a Link Code, sent on
every later character push. The bot persists only a hash of it, never the plaintext. Distinct
from a Link Code: long-lived and repeatable vs. short-lived and single-use.
_Avoid_: API key, access token

**Account Label**:
An operator-chosen name identifying one WoW account (not a Discord account, not a Battle.net
account) within a Linked Account — e.g. `"Main"` or `"Alts"`. One Discord User can hold several,
one per WoW account or per desktop install that has linked.
_Avoid_: account (bare — always ambiguous between a Discord account and a WoW account)

**Linked Account**:
The persisted association between a Discord User and one Account Label: its Device Token hash,
the Character Snapshot it last pushed, and when. Created by redeeming a Link Code; removed by
`/unlink`. A Discord User's links are a list of these, not a single record.
_Avoid_: link, character link (the *act* of creating one is "linking"; the noun is a Linked
Account)

**Character Snapshot**:
The full character-data payload one push replaces its Linked Account's prior snapshot with —
never a diff or an append. Shaped after the desktop app's own `CharDb` (warband gold, and
per-character level/spec/professions/gear ilvl/currencies/playtime/reputations).
_Avoid_: character data (doesn't say "whole thing, replaced wholesale, not merged")

**Plugin**:
A self-contained feature unit the bot installs from the Plugin Index by name: a single-file bundle
exporting `createPlugin(host)`, contributing slash commands (built on a host-named builder so they
land under `COMMAND_PREFIX`), optional scheduler ticks, and an `activate()` that runs once, after
`takeOver()`. It never reaches `config`, the `Client` or `data/` directly — everything comes through
the Host API. The first one is the Warbandeer connector, now the published `@rackbops/plugin-warbandeer`
(the baked-in `src/warbandeer/` was removed in #100). See `docs/adr/0004`.
_Avoid_: module, extension, integration, feature flag, connector (the connector is *a* plugin —
"connector" names the desktop-app feature, not the packaging)

**Plugin Index**:
The published manifest (`plugins.json`, `PLUGIN_INDEX_URL`) listing every available plugin: npm
package, current version, `hostApiVersion`, intents, command names, env keys, an optional
admin-bundle `adminUrl` + `adminApiVersion` (a plugin's own admin-panel tab — epic #123), release notes.
Generated by the plugins repo's CI, cached by the bot at `data/plugins/index.json`; the source of
everything the host needs before login.
_Avoid_: manifest (bare — ambiguous with a package manifest), registry, catalog

**Host API**:
The object handed to a plugin's `createPlugin`: `name`, its declared `env` slice, `dataDir`, a
prefixed `log`, `storage`, `announce`. The only door the host *offers* a plugin — a
declared-dependency boundary, not a sandbox (a command handler's `interaction.client` still reaches
the live `Client`); versioned by the integer `HOST_API_VERSION`.
_Avoid_: context, container, services, host (bare)

**Plugin State**:
`data/plugins/state.json`, written by the bot after activation: per plugin, enabled / installed /
available / configured / active, the error if any, and the notification, snooze, skip and schedule
bookkeeping. Read by `ops/bot-ops.sh status` and the admin panel. **#225:** a plugin taken out of
`PLUGINS` keeps its entry, `enabled: false`, instead of being dropped.
_Avoid_: plugin list, cache

**Admin tab**:
A plugin's own settings UI inside the admin panel — a small piece of browser code (`dist/admin.js`,
exporting `mountAdmin(root, api)`) it may ship instead of relying on the panel's generic flat
env-key fields. Delivered same-origin at `/plugin-admin/<name>.js`, pinned to the plugin's
installed version (`docs/adr/0005`, epic #123, #165).
_Avoid_: settings tab (bare — distinguishes it from the panel's other sections), plugin UI

**AdminApi**:
The bridge object a mounted admin tab receives: `getEnv`/`setEnv` (scoped to the plugin's own
declared keys — a `secret` one is write-only, never returned; `setEnv` goes through the same guarded `POST /api/env` a manual config
edit uses), `getState` (this plugin's `data/plugins/state.json` entry), `proxyFetch` (a data asset
from the plugin's own published package). The panel-side analog of the bot's Host API — the plugin
owns presentation, the panel keeps authority.
_Avoid_: admin api (bare — ambiguous with the panel's own `/api/*` routes), bridge

**Admin bundle**:
The built `dist/admin.js` an admin tab compiles to (`bun build --target browser`, no discord.js) —
served same-origin from `GET /plugin-admin/<name>.js`, fetched by the panel server from the
plugin's own published npm package on the allowlisted CDN host, never loaded cross-origin by the
browser.
_Avoid_: bundle (bare — ambiguous with the bot's own plugin bundle, `dist/plugin.js`)

**Routing**:
Where a plugin lives, as data the bot owns (ADR-0006, Epic #236): per plugin, per Discord server,
which channels its commands work in (`"all"`, or a list) and which channel it posts to. Stored in
`data/routing.json`, written only by the bot; a webhook URL, being a secret, lives in
`data/routing.secrets.json`, which is created owner-only, and in no other file the bot writes (a
corrupt copy moved aside by `readJsonOrFresh`, or the temp file of a failed write, is that same file
under another name, and is owner-only too -- for a file the bot wrote: one put there by hand keeps
its own mode until the bot's first write replaces it). Per plugin, never per command. #237 added the
model (the shapes, the pure decisions in `src/routing/resolve.ts`, the store); #239 wired command
registration to it. With no `routing.json`, or one that places no plugin, the bot registers exactly as
it always did — to `DISCORD_SERVER_ID`, or globally when that is unset — and once a plugin is placed
it registers per server. #241 added the way in: a request the panel drops in the mailbox
(`routing-set`, `webhook-add`, `webhook-remove`, `discovery-refresh`) is validated by the bot against
what it can see and applied without a restart, and its outcome is recorded in `routing.json`'s
`results`; a file seeded by hand still works. #243 made the rest act: a plugin posts to the channels its
routing names (through a channel's webhook when one is registered, as the bot otherwise), and its
commands run only in the channels its routing lists; a plugin whose routing names no channel posts to
`ANNOUNCE_CHANNEL_ID`, as it always has.
_Avoid_: channel config, command permissions (Discord's own per-channel command permissions are a
different thing, which the bot cannot edit — ADR-0006 decision 7)

**Home server**:
The server named by `DISCORD_SERVER_ID`: where a plugin with no entry in `routing.json` lives, posting
to `ANNOUNCE_CHANNEL_ID`. With `DISCORD_SERVER_ID` unset there is no home server and an unplaced
plugin lives in every server — what global registration does today.
_Avoid_: main server, primary guild

**Placed / unplaced plugin**:
A plugin is **placed** when `routing.json` has an entry for it, and lives in exactly the servers that
entry lists; **unplaced** when it has none, and lives in the home server. A placed plugin whose entry
has no servers lives nowhere (see the gotcha "A plugin with an entry in `routing.json` and no
servers lives nowhere").
_Avoid_: enabled / disabled (that is `PLUGINS=` — whether the bot loads a plugin at all)

**Discovery**:
What the bot can see of Discord, published by the bot as `data/discovery.json` so that the panel,
which is not given the Discord token in its environment (`docker-compose.yml`'s `admin` service has no
`env_file:`), can offer servers and channels by name: the servers, their text
channels and whether the bot can post in each, the outcome of the last command registration in each
server it registered commands in, an invite URL, and per plugin whether it posts and which commands it registered.
`DiscoveryFile` in `src/routing/model.ts` is its shape and `validatePluginRouting` checks a request
against one -- the bot's own check of a panel request (#241), which reads the file back with
`readDiscovery` and refreshes it once first if the request names something it does not show. The bot writes it (`src/routing/live.ts`, #239) after every command registration —
including the boot one, whether or not any routing exists — and refreshes it every 15 minutes, and at once when the bot joins or leaves a server (#259) (a write is skipped when the bot cannot read its servers, so the panel keeps the last copy).
_Avoid_: server list, guild cache

## File Map

| File | Responsibility |
|---|---|
| `src/index.ts` | Before the `Client` exists at all: `loadPluginIndex()` fetches the Plugin Index, `selectPlugins()` picks the plugins named in `PLUGINS=` — handed the previous boot's `state.json` version pins (`pinsFromState(await readPluginState(...))`, a second read of the file `activate()` reads again for `installPlugins`) so an older pinned or last-good version can be kept past an index host-API bump (#222) — and `collectIntents()` unions their declared intents onto `CORE_INTENTS` — only then is `createClient()` called, since discord.js freezes intents at construction. Each skip reason from `describeSkips()` is logged (`[plugins] <name>: <reason>`) before the one-line summary. No plugin code loads at this top-level boot block; an unset `PLUGINS` behaves exactly as before (Guilds intent only). Then login, then `activate()` (after `takeOver()`): install + load the selected plugin bundles (`installPlugins` → `loadPlugins`, the only place a plugin bundle is `import()`ed — after the handoff; each plugin's `HostApi.announce` is `postForPlugin(entry.name, …)` over one `postDeps` built once in `activate()` — #243: the channels its routing names, through a channel's webhook when it has one, `ANNOUNCE_CHANNEL_ID` when routing names none), slash-command registration built from core + plugin commands (`buildCommandBody`) and carried out by `initRouting` + `applyRouting("boot")` (#239, `src/routing/`): with no `routing.json`, or one that places no plugin, the one PUT it always made (guild if `DISCORD_SERVER_ID`, else global) followed by the unchanged `Registered N slash commands` line, inside the same `try`/`catch` that keeps a registration failure from taking the bot down; once a plugin is placed, one PUT per server and one `Registered commands in N servers (...)` line, the interaction routes (chat commands → `handleCommand` with a plugin-command lookup and, since #243, a channel gate — `gateCommand`, which calls `whereOf` only when the channel's own id is not one the plugin's routing lists; core commands never reach it — `/report` modal submits → `handleReportModal`), the `guildCreate` / `guildDelete` listeners (#259: `guildJoined` / `guildLeft`, attached before the registration so a join during it queues behind it on `live.ts`'s chain; the availability pair is deliberately not wired), `activatePlugins()` (which flips each plugin's `running` gate once its `activate()` succeeds), then the scheduler (with `pluginTicks(loaded)`) and, right after it, `startRequestDrain` (#241: the request mailbox every five seconds, each drain inside `withCritical`, its beats gated on `isPluginStateReady` and `restartPending`, so none runs before the boot drain below or on the way out) — so the scheduler's first synchronous tick already runs plugin ticks at boot with `running:true`, the gate kept as defence in depth, `writePluginState()`, and `reportUpdateOutcome()` fired un-awaited (an owed `/update` follow-up must not delay startup), and `reportPluginUpdateOutcome()` likewise (#104 — the owed `/plugins update` follow-up; DM→channel, clearing `pendingReport` via the shared mutator before delivering). **#105:** `consumePluginRequests(livePluginRequestDeps())` is **awaited before `markPluginStateReady()`** (not fire-and-forget) — so a request dropped while the bot was down is honoured before any tick or `/plugins` command interleaves, and the single-flight consumer never overlaps the first tick's drain. Under `resolveBootMode() === "standby"` (nazumods/wow#879, confirmed against the daemon since #46) it logs in and attaches **none** of that until `takeOver()` has retired the original — both instances hold the same token and Discord delivers events to both sessions, so a standby with handlers would double every reply. A standby that never reaches `ClientReady` inside `VERIFY_DEADLINE_MS` writes a `failed` marker and exits 1, so the original gets a reason rather than a timeout. After `writePluginState` it calls `markPluginStateReady()` (unconditionally, even on a write failure) to release the `pluginUpdates` tick (#103) now that it can't race the boot write |
| `src/client.ts` | `CLIENT_OPTIONS` (the mention-safe default, #48) and `createClient(intents = CORE_INTENTS)` — `CORE_INTENTS` (`[Guilds]`) is what an unset `PLUGINS` still gets; `src/index.ts` instead passes `collectIntents(CORE_INTENTS, selectedPlugins)` once plugins are selected. `CLIENT_OPTIONS.intents` itself is never read directly by `createClient` (always overridden by the parameter) — it stays in place only because `CLIENT_OPTIONS` is also the object pinned whole by `client.test.ts`'s `allowedMentions` check |
| `src/config.ts` | Env config from `.env` — pure `resolveConfig(env)` (exported for tests) + the `config` singleton resolved from `process.env`; throws at import time on missing required vars or an invalid `COMMAND_PREFIX` (the WoW config — region, `DMF_TIMEZONE`, realm, Blizzard creds — moved to `@rackbops/plugin-wow` in #107, which validates them the same way when it loads). Also the `/report` project→repo map (`REPORT_PROJECTS` + `repoForProject`) + `reportRoleId`, the release-watch list (`watchedRepos` from `WATCHED_REPOS`, comma-separated `owner/repo`, defaulting to `[githubRepo]` — distinct from `githubRepo`, which anchors self-update), and the self-update config (`gitSha`, `botBranch`, `autoUpdate`, `adminUserIds`); plugin selection — `plugins` (`PluginSelector[]`, from `PLUGINS=` — bare `name` or `name@version` to pin, validated and deduped by name) and `pluginIndexUrl` (from `PLUGIN_INDEX_URL`, defaulting to the Rackbops/rackbops-bot-plugins repo's published manifest — accepts `http(s)://`, `file://`, or an absolute path) |
| `src/commands.ts` | `/update`, `/report`, `/plugins` command builders + dispatch (`/dmf`, `/reset`, `/status`, `/transmog` are the `wow` plugin's since #107, and `/link`/`/unlink` the `warbandeer` plugin's — all routed through `handleCommand`'s plugin lookup, below); `cmd(name)` builds every command under `config.commandPrefix` (via `commandNamer(prefix)`, now in `src/commandNaming.ts` so `src/plugins/host.ts` can name plugin commands the same way WITHOUT importing `commands.ts` — the #104 cycle break that lets `commands.ts` import `host.ts`'s state mutator), `bareName()` strips it back off on dispatch; `handleCommand(interaction, lookup?, gate?)` runs a core command first, and never puts it to the gate; a non-core one goes to `lookup(bare)` — a loaded plugin's command — after `gate(bare, interaction)` (#243: a string is the private, ephemeral refusal shown instead, `undefined` lets it run, and a gate that throws lets it run), or warns if nothing matches; `isAdmin()` allowlist gate for `/update`; `CORE_COMMAND_NAMES` (bare `commandData` names, via `bareName()`) is what `selectPlugins` (`src/plugins/registry.ts`) checks a plugin's declared command names against before any plugin loads; `updateReply(decision, latestSha, { runningSha, reason, redeploy })` (exported for tests) renders the `/update` acknowledgement — a `redeploy` result present at all means the swap **failed**, since a successful one never returns to reply (the replacement retires this process and delivers the ✅ itself) — the running sha is a **parameter, not a `config` read**, because config resolves env at import time and is one singleton for the whole bun test process, so a formatter reading it is only testable by winning a race with whichever test file imports config first; dispatch passes the interaction's user/channel/application/token to `checkForUpdate` as the follow-up requester; Discord `<t:…>` timestamp helpers. Also the admin-gated `/plugins` command — subcommands `list` (#103) + `update <name> [at]` / `remind <name> [days]` / `skip <name>` / `cancel <name>` (#104), all deferred + ephemeral, gated once before the subcommand switch. `list` renders `renderPluginsList`; the four actions validate against the index + `state.json` then `mutatePluginState` (the shared race-safe mutator) via pure `planPluginAction`. `update` NOW mirrors `/update`: pin write + reply inside `withCritical`, then `requestRestart` (the exit deferred to the critical-section end, so the write and reply land first); `update at:<HH:MM UTC | ISO-8601+offset>` stores a `scheduled` entry instead |
| `src/routing/post.ts` | Where a plugin's `announce` goes (#243). `postForPlugin(plugin, message, deps)` takes `announceTargets` and, per target and in order, posts through the channel's webhook when `routing.json` has metadata for it that is not `broken` and `routing.secrets.json` has a URL for it, and as the bot (`announceTo`) otherwise. A webhook that fails, for any reason, falls back to the bot for that post; only a 401 or 404 (`gone`) also marks it broken (`markWebhookBroken`, through the pure `withWebhookBroken`: `webhooks[channel].broken` in `routing.json`, only on the entry that was posted through — same `id` and `addedAt` — with `updatedAt` and `updatedBy` untouched). It rejects only when every target failed, with the first error; the others are logged. `liveExecuteWebhook(fetch?)` POSTs `{ content, allowed_mentions: { parse: [] } }` with a 10 s timeout and returns fixed reasons only. The secrets file is read only when a target could use it, and a routing or secrets read that fails posts to the default channel or as the bot. Pure over injected deps; tested in `post.test.ts` (with a token-search test that finds the URL in no log line, error or `routing.json`). |
| `src/routing/gate.ts` | The channel gate (#243). `whereOf(interaction, fetchChannel)` is the only function that touches discord.js objects: a thread's `parentId` is its channel's, an uncached channel is fetched once, and a fetch that fails (or a thread with no parent) sets `parentUnknown`. `refusalMessage(command, channels)` words the refusal, e.g. `` `/rsetlist` works in <#1> or <#2> here. `` (five channels, then "and N more"). `gateCommand(plugin, commandName, here, whereOf, readRouting, log)` returns the refusal to show or `undefined`, and never throws: the channel's own id decides first, and `whereOf` (a thunk, so the fetch is made only if needed) is called only when that id is not listed; a DM, no plugin (a core command), no entry for the server, `"all"`, a routing read that throws and an unknown parent all let the command run, and the last two leave a `[gate]` line. Tested in `gate.test.ts`. |
| `src/commandNaming.ts` | Just `commandNamer(prefix)` (a `SlashCommandBuilder` factory), extracted from `commands.ts` in #104 so `src/plugins/host.ts` imports it here instead of from `commands.ts` — removing the one host→commands edge, which is what lets `commands.ts` import `host.ts`'s shared `mutatePluginState` (the `/plugins` write path) without a cycle. Depends only on discord.js |
| `src/report.ts` | `/report` flow: role gate (reads roles from the interaction — no Members intent), Title/Description modal, then `createIssue` in the mapped repo labeled `automated`; `reportBody` footer names the reporter (plain username, no mention); pure `reportAnnouncement()` renders the channel-visible confirmation (reporter, `repo#N` + url, title, description clamped to Discord's 2000-char cap with a truncation note) |
| `src/announce.ts` | 60 s tick scheduler: release polling (`checkReleases` loops `config.watchedRepos`, isolating each repo's failure via a per-repo try/catch so one bad repo can't starve the others; a repo `fetchReleases` reports unreachable is skipped without touching its seen-id list — so it seeds silently if it returns — and warns only on the edge, via the module-level `releaseReachability` log); routes releases via `channelFor()` → `RELEASE_ANNOUNCE_CHANNEL_ID` (falls back to `ANNOUNCE_CHANNEL_ID`); `AnnounceKind` is now just `"release"` (the WoW announcement kinds moved to the wow plugin, #107, though the `channelFor`→`announceTo` seam stays for nazumods/wow#528). `runTick(checks)` (issue #43, exported) generalizes `checkReleases`'s own per-repo isolation to the whole tick — `onTick` runs the checks through it, so one throwing can no longer starve the rest of that tick. The tick set comes from `tickChecks(client, extra)` (exported, pure): the core checks in order (`releases`/`autoUpdate`/`pluginRequests`/`pluginUpdates`/`discovery`) then any plugin ticks (the DMF / weekly-reset / realm checks are the wow plugin's now, #107); `startScheduler(client, extraChecks = [])` threads them in from `src/index.ts`'s `pluginTicks(loaded)`. `announceTo(client, channelId, message)` is split out of `announce()` (the `[announce]` log line unchanged) so a plugin's `HostApi.announce` reuses the exact send path. `TickCheck` now comes from `src/plugins/contract.ts` (imported type-only), not declared here. Also the `pluginUpdates` core check (#103, after `autoUpdate`): a 15-min-gap + startup-catch-up tick that runs `checkPluginUpdates` to DM admins about newer plugin versions **and (#104) execute a due scheduled update — which restarts the bot** — gated on `markPluginStateReady()` (set by index.ts after the boot `state.json` write) so its keyed-mutator persist can't race that write; `livePluginUpdateDeps(client)` wires its injected deps, now including `restartPending`/`requestRestart` from `restart.ts` (so `updates.ts` itself stays restart-free). **#105:** a `pluginRequests` core check runs BEFORE `pluginUpdates` (so a just-queued request applies the same tick), draining the mailbox every tick via `consumePluginRequests(livePluginRequestDeps())` (real fs + the shared index/state/mutator, no client — a panel request has no DM; since #241 the same deps carry a `routing` block -- the routing store, `applyRouting` / `refreshDiscovery`, and the webhook lookup -- and the mailbox is also drained every five seconds by `startRequestDrain`, so this tick check is the backstop); gated on `pluginStateReady` like `pluginUpdates`. And the `discovery` core check (#239, after `pluginUpdates`, before the plugin ticks): a 15-min-gap + startup-catch-up tick (`shouldRefreshDiscovery`, the same shape as `shouldPollReleases`) that QUEUES `refreshDiscovery()` from `src/routing/live.ts` to re-snapshot `data/discovery.json` and does not wait for it — the plugin ticks after it must not wait on a registration's PUTs, which is why `index.ts` starts the scheduler before registering — so on the first tick the refresh lands behind the boot registration; a refresh that rejects is caught and logged (`[tick:discovery]`); before `initRouting` it is a no-op. |
| `src/config.test.ts` | bun tests for `resolveConfig` (release-channel fallback, required-var, `COMMAND_PREFIX`, `REPORT_ROLE_ID`, `WATCHED_REPOS` parse/default, self-update vars, plugin selection — the WoW region / `DMF_TIMEZONE` cases moved to the wow plugin's own config tests) + report helpers (`repoForProject`, `reportBody`, `reportAnnouncement` — content, no-truncation-when-it-fits, the 2000-char clamp, and the boundary either side of it) |
| `src/announce.test.ts` | bun tests for `runTick` (issue #43) — the remaining tick checks (`checkReleases`/`checkAutoUpdate`/the plugin-request/update passes) call real discord.js/GitHub APIs and aren't exported, so this exercises the extracted isolation mechanism directly: a throwing check doesn't stop the rest, the failure is logged naming which check failed, checks run in the given order, and a later throw doesn't retroactively affect an earlier check that already completed. Also `tickChecks` (the core checks' order and, since #239, the `discovery` check: its gap, that it queues its refresh instead of waiting on a registration in flight, that a rejecting refresh is caught), the `shouldPollReleases` / `shouldRefreshDiscovery` boundaries, `announceTo`, `guardedTick` and `commitReleaseAnnouncements` |
| `src/github.test.ts` | bun tests for pure `decideReleaseAnnouncements`: silent seed on never-polled, unseen-only oldest-first, no-op when nothing new, first release of a zero-release repo. Plus `fetchReleases` against a stubbed `globalThis.fetch` (payload mapping, draft filter, tag fallback, `null` on 404, `[]` on a releaseless repo, throws on 401/403/429/5xx) and `createReachabilityLog` (once per standing failure, silent when healthy, recovery, per-repo independence) |
| `src/state.test.ts` | bun tests for pure `normalizeSeenReleaseIds`: legacy-array migration under the default repo, empty array → `{}`, keyed map/`undefined` pass-through. Real-temp-dir coverage (never the actual `data/state.json`) for `loadStateFrom`/`saveStateTo`/`createStateWriter`: an absent file loads fresh, a well-formed one round-trips, an empty or malformed-JSON file loads fresh with a logged warning and gets moved aside rather than thrown, and 100 concurrent `save()` calls through one writer leave a parseable file |
| `src/commands.test.ts` | bun tests for `isAdmin` (allowlist hit/miss, fails closed, whole-id match) + `bareName()` (strips the prefix, no-op when unset, passes an unprefixed name through unmangled) + `updateReply()` (names the target build; no longer asks the reader to check whether it changed; both `disabled` reasons — a missing `GIT_SHA` vs. an unpublished one, which is named) |
| `src/update.ts` | Self-update: pure `decideUpdate()` + `sameSha()`, `fetchLatestBotSha()` (newest `config.botBranch` commit on `config.githubRepo`), `fetchShaRelation()` (the `ShaRelation` ancestry answer from `GET /compare/{latest}...{running}`; 404 → `unpublished`, any other failure → `unknown`, never throws), stateful `checkForUpdate({ force, requester })` answering `busy` while a handoff is in flight (guard before any state write — see the gotcha), returning a `DisabledReason` when it disables — and, on `restart`, routing through `applyUpdate()`: a self-contained `redeploy()` when the daemon socket is mounted, else the original exit-75 fallback so a deployment on an older compose file keeps working. A failed redeploy clears `attemptedUpdateToSha` + `pendingUpdateReport` again, since the restart never happened — leaving them would owe a follow-up for nothing and let the anti-loop guard suppress the next genuine attempt; pure `buildUpdateReport()` returns the `PendingUpdateReport` to persist across the restart, or `undefined` when there's no requester (which is what keeps an `AUTO_UPDATE` exit silent) |
| `src/update.test.ts` | bun tests for `decideUpdate`/`sameSha`: staleness, short-sha prefixes, anti-loop suppression, `force`; the ancestry matrix (`ahead`/`identical` → current, `behind`/`diverged` → restart, `unpublished` → disabled and outranking both suppression and `force`, `unknown` → pre-nazumods/wow#871 fallback, equality shortcut winning before any relation is read); `fetchShaRelation` against a stubbed `fetch` (each status, 404, 500, a rejected fetch, an unrecognised status, and the `latest...running` argument order); plus `buildUpdateReport` (records requester + both shas; no requester → no report) |
| `src/updateReport.ts` | The follow-up owed after a `/update`-initiated restart. Pure `decideUpdateOutcome()` (`updated`/`noop`/`unexpected`/`unknown` by comparing the running `GIT_SHA` against the report's `toSha`/`fromSha`), `updateOutcomeMessage()`, `tokenUsable()` (15-min interaction-token window), `reportTooOld()` (24 h); `deliverUpdateReport()` walks interaction follow-up → DM → channel with injected deliverers, logging and falling through on each failure; `reportUpdateOutcome(client)` is the boot entry point |
| `src/updateReport.test.ts` | bun tests for the four outcomes (incl. short-sha tolerance and a missing `GIT_SHA`), per-outcome message content, the token/staleness windows, and the delivery fallback order — including "every route fails" resolving to `none` rather than throwing |
| `src/restart.ts` | Ref-counted critical section + `requestRestart()`; exits `RESTART_EXIT_CODE` (75); `setExitFn`/`resetForTest` for tests. Also `beginHandoff()`/`endHandoff()`/`handoffActive()` (nazumods/wow#879): quiesce for a handoff — `restartPending()` goes true so the scheduler stops writing `state.json` while the replacement shares that volume — but the process deliberately does **not** exit, because it is the only thing that can remove a replacement that fails to verify. `redeploy()` calls `beginHandoff()` only just before creating the replacement, not before the build ahead of it (#130). **#154:** a private `handoffExemption()` is computed LIVE on every check — 1 while a handoff is active, 0 otherwise, never a snapshot taken at `beginHandoff()` time (an earlier, reverted version did that — see the Gotchas entry for why it was wrong); `awaitCriticalIdle(timeoutMs)` resolves once `critical` falls back to (not below) that live exemption, or `false` on timeout. `beginShutdown()` folds a `shuttingDown` flag into `restartPending()` so no new tick starts once a signal has arrived |
| `src/shutdown.ts` | **#154:** the first (and only) signal handler in this codebase. `createShutdownHandler(deps)` is a pure factory (fake `beginShutdown`/`awaitIdle`/`disposePlugins`/`destroyClient`/`exit`/`log`/`graceMs` in tests) returning the real `(signal) => Promise<void>` handler `index.ts` registers for both `SIGTERM` and `SIGINT` — the SAME instance for both, so a second signal (either name) is recognised as "already draining" and exits immediately rather than starting a second drain. `SHUTDOWN_GRACE_MS` (8s) is the TOTAL budget — the drain, `disposePlugins`, AND `destroyClient` together, nested (each bounded to whatever's left of `graceMs` after the previous step, `disposePlugins` capped at `DISPOSE_PLUGINS_TIMEOUT_MS` and `destroyClient` at `DESTROY_CLIENT_TIMEOUT_MS`), not additive — leaving real margin under `docker stop`'s 10s SIGKILL default; see the Gotchas entry for why an earlier, reverted version got this wrong. **#184:** order is drain critical sections -> `disposePlugins` (`plugins/host.ts`, releases what each running plugin's `activate()` acquired) -> `destroyClient` -> `exit` — plugins get a chance to close servers/handles they opened while the process and its gateway connection are still alive |
| `src/restart.test.ts` | bun tests: immediate vs deferred exit, nesting, exit-once, release-on-throw; handoff quiesce-without-exit, resume, idempotent resume, and that a handoff neither masks nor is masked by a genuine pending restart. **#154:** `awaitCriticalIdle`/`handoffExemption` — resolves immediately at the exemption (the auto-update case) and with no handoff at all; the mutation-guard row against a naive compare-to-zero; the review-finding scenario itself (a handoff holder plus a concurrently-open unrelated tick, both open at handoff time, must wait for the tick to close, not resolve immediately); waiting for a section opened after the handoff began; two unrelated sections that must BOTH close before resolving (guards `maybeResolveIdle`'s own condition, not just `handoffExemption`); the timeout path; and that the exemption drops to 0 at `endHandoff` |
| `src/shutdown.test.ts` | **#154:** bun tests for `createShutdownHandler` via a fully-faked `ShutdownDeps` harness (its `order` array lets a test assert relative call order across dep fakes) — drains then exits 0; actually awaits the drain before exiting (does not exit early); grace-elapsed-but-still-exits; a second signal (either `SIGTERM`/`SIGINT`) while draining exits immediately without a second `beginShutdown`/`awaitIdle` call, bounded by a `Promise.race` so a regression fails fast instead of hanging the suite; a `destroyClient` rejection is swallowed; a `destroyClient` that never resolves does not hang the exit; `SHUTDOWN_GRACE_MS` stays under 10s; and that `destroyClient` is bounded to whatever's actually left of `graceMs` (not a fresh `DESTROY_CLIENT_TIMEOUT_MS` stacked on top) — the row that guards the nested-vs-additive timing fix. **#184:** order is `drain -> disposePlugins -> destroyClient -> exit`; a `disposePlugins` rejection is swallowed and a never-resolving one doesn't hang the exit (mirrors `destroyClient`'s own two rows); all three phases stay bounded to one `graceMs` total, not additive; and — the narrower regression the first "not additive" test alone couldn't catch — `destroyClient`'s own remaining-budget calculation accounts for time actually spent in `disposePlugins`, not just the drain (a version measuring only drain-elapsed time passed the looser test but failed this one, so both stayed) |
| `src/docker.ts` | Minimal Docker Engine API client over `/var/run/docker.sock` via Bun's `fetch(url, { unix })` — no dependency. Unversioned paths so it tracks the daemon's own API version. `inspectSelf()` (hostname first, `parseContainerId()` off `/proc/self/mountinfo` as the fallback for a pinned `hostname:`), `buildImage()` (remote git context — the daemon fetches the source, so the image needs no git or tar), create/start/stop/remove/rename, image list/remove, `tagImage()` (moves a tag onto another image by name/id — no re-pull or re-build; splits a `repo:tag` target into the two separate query params the Engine API wants), `daemonReachable()`. Every exported call runs inside `bounded(ms, what, fn)` (#130): one `AbortController` + ref'd `setTimeout` wrapping the **whole operation, request and response-body read together**, disarmed by `clearTimeout` only once it settles (`DEFAULT_TIMEOUT_MS` 60s; `buildImage` overrides to `BUILD_TIMEOUT_MS` 15min, and every call takes an optional `timeoutMs` so each is testable). Bounding the body matters as much as the request — a daemon that answers with headers then wedges mid-body hangs just as hard, and for `/build` the streamed JSONL body IS the build. Deliberately **not** `AbortSignal.timeout()`: Bun backs that with an **unref'd** timer, so whether it fires at all depends on something *else* holding the event loop open. At runtime something usually does (the scheduler's 60s interval; `resolveBootMode`'s own ref'd `withTimeout` covers the pre-login window where no scheduler exists yet) — but that is an unenforced invariant of unrelated code, and it does not hold under test, which is where the gap was found. A ref'd timer needs no such reasoning. Pure `parseBuildOutput()` because `/build` answers **200 even when the build failed**, with the error buried in the JSONL stream |
| `src/docker.test.ts` | bun tests for `parseContainerId` (both mountinfo layouts, short-id rejection) and `parseBuildOutput` (the 200-with-an-error case, `errorDetail.message` preference, non-JSON noise), plus stubbed-`fetch` daemon calls: ping, build query construction (remote + tag + build-arg), `tagImage`'s repo/tag query-param split, and 304/404 on stop/remove being success rather than errors. #130's bound is pinned by a **table driving all 14 exported daemon calls** two ways — a daemon that never answers, and one that answers then wedges mid-body (the stub errors the body stream on abort, as a real `fetch` body does; a hand-built `Response` ignores the signal and can't model it). Every bound assertion goes through `settleWithin()`, which races a **ref'd** timer: a regressed bound leaves nothing ref'd, so bun's own per-test timeout can't interrupt and the suite would wedge with no output until the CI job limit instead of going red. Plus a completed call **clears** its timer rather than leaving one armed, and a value pin on `DEFAULT_TIMEOUT_MS`/`BUILD_TIMEOUT_MS` (no production caller passes an explicit timeout, so nothing else would catch raising them) |
| `src/handoff.ts` | The handoff contract, kept free of *daemon* I/O so it tests as pure logic: `bootMode(env)` (presence of `HANDOFF_FROM` is the *raw* standby instruction — necessary but not sufficient for the boot's actual mode since #46, see `redeploy.ts`'s `resolveBootMode()`), the marker file in the shared volume (its own file, never `state.json` — one writer, one reader), `decideHandoffOutcome()` → `ready`/`failed`/`timeout`/`waiting` (plus `stalled`, decided not by it but by outliving `RETIREMENT_DEADLINE_MS` — see the gotcha), deadlines, `handoffFailureMessage()`. The marker is read before the container state on purpose: a replacement that signalled `ready` is already stopping the original, so what it *said* must outrank how it currently looks |
| `src/handoff.test.ts` | bun tests for `bootMode` (incl. an empty `HANDOFF_FROM` staying normal), the full `decideHandoffOutcome` matrix, the three ready-marker-wins races (mid-stop, already gone, expired deadline), and `handoffFailureMessage` always saying the current build is still running |
| `src/redeploy.ts` | The swap itself. `resolveBootMode(env)` (#46) is what `index.ts` actually calls to decide its boot mode: `bootMode(env)`'s raw "standby" is confirmed against the daemon — `HANDOFF_FROM` is baked into the replacement's env at creation and never cleared, so without this every later in-place restart of the swapped-in container (`bot-ops.sh restart`, the ops panel, a reboot, a crash loop) would re-read "standby" forever against a dead original; downgrades to `normal` only once the named original is confirmed gone (a 404, not merely not-running — see the gotcha on why running-state would race a host reboot), the one daemon call it makes is bounded by `RESOLVE_BOOT_MODE_TIMEOUT_MS` (10s, well under `VERIFY_DEADLINE_MS`) since this is the first daemon dependency on the path *before* `client.login()`, and it stays `standby` on any unconfirmed daemon error (including that timeout) rather than risk two live bots. `redeploy(sha)`: build (`buildRemote()` = `…/<repo>.git#<branch>`, the whole repo at the target branch, tagged **only** `shaTag()` — never `:latest` at build time, see the gotcha) → prune → **quiesce** (`beginHandoff` here, not before the build — the build writes no state and is the one long call, #130) → create + start the replacement from that same sha tag → poll until `decideHandoffOutcome()` settles; **returns only on failure**, since a successful handoff kills this process mid-await — observing `ready` starts a bounded wait for that death, and outliving it demotes the outcome to `stalled` (reclaim + report) rather than quiescing forever. Pure `buildCreateSpec()` derives the replacement entirely from the original's own inspect (compose labels replicated, mounts re-bound, `User` carried over, `GIT_SHA` **dropped** so the new image's baked sha wins), `shaTag()`/`latestTag()`, `selectImagesToPrune()` (sha-shaped tags of this repo only — `:latest` and hand-named rollback targets are never candidates), `replacementName()`. `takeOver()` (runs in the replacement, from `index.ts`'s `ClientReady`): announces `ready`, applies `latestTag()` onto its own now-verified image via `tagImage()` (best-effort — a tag hiccup must not fail an otherwise-successful handoff), then `retireOriginal()` — stop + **remove** (an exited container would be respawned on its old image by `restart: unless-stopped`) then take the canonical name. `retireOriginal()` inspects the original *before* stopping it to tell a genuine first attempt from a call against an original with nothing left to reclaim it — already stopped, or gone entirely (see the gotcha) — a first attempt lets a genuine `stopContainer` failure propagate, everything else degrades one instead |
| `src/redeploy.test.ts` | bun tests for naming, `buildRemote`, `shaTag`/`latestTag`, `selectImagesToPrune` (keep-newest-N oldest-first, the never-prune set, untagged images), `buildCreateSpec` (label replication, `HANDOFF_FROM` injection with no stacking, the `GIT_SHA` drop, both binds, `User`, restart-policy default, network), `takeOver` (ready/tag/retire/clear ordering, a `tagLatest` failure not aborting the handoff), `retireOriginal` (a first-attempt stop failure propagates, a retry-against-a-corpse stop failure is tolerated and removal still runs, the ordinary 304 retry path is unchanged, a fully-gone original tolerates a genuine stop failure too and still returns cleanly on the expected 404), and `redeploy()` end-to-end via a stubbed `fetch` — including that a failed/timed-out handoff never touches `:latest`, the build request itself carries only the sha tag, and (#130) that the scheduler is still live at `/build` but quiesced by `/containers/create`, and that a failed build leaves it un-quiesced because `beginHandoff` never ran; `resolveBootMode` (#46) — no `HANDOFF_FROM` or an empty one stays `normal` without ever calling the daemon, a running original keeps `standby`, a confirmed-gone (404) original demotes to `normal`, an original that exists but isn't running yet **stays** `standby` (not `normal` — pins the check to existence, not running-state), and both a thrown daemon error and one that never resolves (timeout) stay `standby` |
| `src/state.ts` | Announcement dedup state, persisted to `data/state.json` (gitignored). `seenReleaseIds` is keyed per `owner/repo`; pure `normalizeSeenReleaseIds()` migrates the legacy global array (single-repo era) under `config.githubRepo` on load; `saveState` caps each repo's list at 100. `attemptedUpdateToSha` guards the self-update exit loop; `pendingUpdateReport` (`PendingUpdateReport`: `fromSha`, `toSha`, `userId`, `channelId?`, `applicationId?`, `interactionToken?`, `requestedAt`) is the `/update` follow-up owed on next boot — purely additive, so an old `state.json` simply lacks the key. `loadStateFrom`/`saveStateTo`/`createStateWriter` (issue #42) are the path-parameterized primitives `state`/`saveState` are thinly wired onto: writes go through a temp file + atomic rename, serialized behind a per-writer promise chain (`withCritical` guards a *restart* firing mid-write, not two writers racing each other); a present-but-corrupt file is logged and moved aside to `<path>.corrupt-<ts>` rather than thrown from the top-level `await` that binds `state` at import |
| `src/wow/*` | REMOVED in #107 — the WoW features (DMF/reset/realm/blizzard/transmog modules, the 4 commands, the 3 ticks) are now `@rackbops/plugin-wow`, published from `Rackbops/rackbops-bot-plugins`. |
| `src/github.ts` | GitHub API client: `fetchReleases(repo)` (drafts filtered; returns `null` on a 404 — the repo is missing or invisible to `GITHUB_TOKEN`, which GitHub reports identically — while 401/403/429/5xx still throw, and a releaseless repo is a plain `[]`) + `createReachabilityLog()` (edge-triggered per-repo tracker: `observe(repo, reachable)` → `"lost"` / `"recovered"` / `null`, in-memory so a restart re-reports once) + pure `decideReleaseAnnouncements(releases, seen)` (seed-silently on `seen===undefined`, else announce unseen oldest-first) + `createIssue` / idempotent `ensureLabel` for `/report` (both need `GITHUB_TOKEN` with issues:write). All five calls carry `AbortSignal.timeout(GITHUB_TIMEOUT_MS)` (10s, overridable per call so each is independently testable) since #88 — the budget is **aggregate**, since `checkReleases` walks `config.watchedRepos` serially: `10N + 30` seconds, so **26** watched repos is the ceiling before a tick's GitHub budget alone reaches `TICK_WATCHDOG_MS` and re-opens #87's duplicate-announcement race. Not tighter than 10s: a timed-out `fetchShaRelation` degrades to `"unknown"`, which `decideUpdate` resolves to `"restart"` — a real self-redeploy |
| `Dockerfile` | `oven/bun:1-slim` (Debian — Intl IANA timezones), prod-only install, non-root `bun` user, `VOLUME /app/data`, `ARG/ENV GIT_SHA`, `ENTRYPOINT entrypoint.sh` |
| `entrypoint.sh` | Root→`bun` drop when compose starts the container as root: joins the socket's group by reading its GID off the socket (`setpriv --groups`), execs the CMD; a plain exec under `USER bun` |
| `docker-compose.yml` | Local dev: `GIT_SHA=$(git rev-parse HEAD) docker compose up -d --build` — `build.context`/`container_name`/`env_file` default to `.`/`warbandeer-discord`/`.env` respectively. A deployed instance overrides all three via `BOT_BUILD_CONTEXT`/`BOT_OPS_CONTAINER`/`BOT_ENV_FILE`, either per-invocation on the command line or (issue #41) from the stack directory's own generated `.env` — never persisted in the bot's own config `.env` (see the config-dir gotcha). `GIT_SHA` build arg, named volume `state` → `/app/data`, `restart: unless-stopped`. Opt-in `cloudflared` sidecar (`profiles: [tunnel]`, needs `CLOUDFLARE_TUNNEL_TOKEN`) for exposing a future local API without inbound firewall ports. Opt-in `admin` sidecar (`profiles: [admin]`, deploy-only — see `ops/admin/`) |
| `ops/docker-compose.test.ts` | bun tests that spawn a real `docker compose ... config --format json` against a throwaway copy of this file (skips loudly if `docker`'s `compose` plugin is absent — true on this maintainer's Windows box, present on GitHub Actions `ubuntu-latest`, so CI is where it actually runs): a stack directory holding `ops/install.sh`'s own generated `.env` resolves `container_name`/`env_file` to that instance's real values, not the monorepo-era fallback (issue #41); a stack dir with only a plain, `BOT_OPS_*`-less `.env` (the local-dev shape above) keeps the intentional `warbandeer-discord` default. Proves the claim instead of just asserting it — issue #41's reopening noted its first acceptance bullet had never actually been executed against the merged file |
| `ops/bot-ops.sh` + `ops/README.md` | Operator admin surface originally driven by the **Ops** tab in `apps/warbandeer-desktop` / `wow-companion` (neither is part of this repo — see **Fork gotchas**): whitelisted `status`/`logs`/`restart`/`env-get`/`env-set` over docker+`.env`, invoked over SSH. Takes `BOT_OPS_CONFIG_DIR` + `BOT_OPS_COMPOSE_FILE` + `BOT_OPS_PROJECT` + `BOT_OPS_CONTAINER` as required inputs (see the config-dir gotcha) — no fallback to its own script location or to any built-in name. Core secrets are never read/written (the whitelist excludes them; a plugin's own `secret` keys are the one exception, write-only — see the #240 gotcha); env-set backs up to `<config-dir>/backups/` then `up -d --force-recreate`. `env-get`, and `env-set`'s change detection, read `.env` with compose's `env_file` semantics via `load_env_values` (last occurrence wins, `export` honoured, whitespace/CR and one layer of matching quotes stripped — issue #44); whitelist membership is checked for every submitted key regardless, but a value's FORMAT is only validated against its regex when the key actually changes. `PLUGINS`/`PLUGIN_INDEX_URL` are whitelisted too, and `env-get`/`env-set` additionally accept every plugin's own env keys — the keys of EVERY plugin in the bot's cached `data/plugins/index.json` (the CachedPluginIndex wrapper, so `.index.plugins`, not a bare `.plugins`), whether or not that plugin is in `PLUGINS` (#256: the index, not `PLUGINS`, decides which keys are plugin keys, so one `env-set` can turn a plugin on AND set its settings, and a key of a plugin that is off is inert until it is on), merged at runtime with the format/`required`/`secret` the Plugin Index carries (#101), so a plugin's key is validated without hand-mirroring it here (`WARBANDEER_INGEST_PORT` moved off the static whitelist to here once #100 removed the baked-in connector); `secret` plugin keys are write-only since #240 (`env-set` may write one, `env-get` never lists it, `env-schema` reports only `secret: true` + `isSet`); an unreadable index degrades to static-keys-only with a stderr note, never an error (and since #256 the index is read on EVERY `env-get`/`env-schema`/`env-set`, even with no `PLUGINS` — one `docker exec … cat`, and the note when the bot has not cached the index yet); `status` gains a `plugins` array read from `data/plugins/state.json`. Also `plugin-request` (#105): read one request JSON on stdin, `jq`-validate action/plugin/version/at/days, and write it into the bot's `data/plugins/requests/<epoch-ms>-<action>-<rand>.json` via `docker exec -u bun` — the **first docker-exec WRITE + first `-u bun`** in the script (every other exec is a read-only root `cat`; the write must be `-u bun` or the bot, running as `bun`, couldn't delete the file it consumes). The filename is script-controlled; the untrusted JSON is only ever the `cat >` body. **The write is atomic and owner-only** (#240, found by #241's review gate): the body goes to `<file>.tmp` in the same directory and is `mv`ed to `<file>`, because `cat > <file>` creates the file before filling it and the bot's drain (which lists `*.json` and rejects one that will not parse) could read it half-written after the panel was already told `queued` — the temp name must never end in `.json`; a failed write removes the temp file and the script dies (a 502 for the panel) instead of reporting `queued` — with status 1 and a line of its own after docker's stderr, where older versions exited with docker's own status; and `umask 077` (after `mkdir -p`) keeps a request that may carry a webhook URL owner-only, which the bot, running as `bun` like the write, can still read and delete. **#240** (ADR-0006) adds `routing-get` — `{routing, discovery}` read from `data/routing.json` and `data/discovery.json` (a missing, empty or corrupt file is `null`; `url`/`token`/`secret`/`password` members and any webhook-URL-shaped string are scrubbed; the bot's webhook store is never opened or named) — and four more `plugin-request` actions, `routing-set`, `webhook-add`, `webhook-remove` and `discovery-refresh`, each validated per action before the file is written (a `webhook-add` URL travels on stdin only and is never echoed). Still fully usable standalone by SSHing in and invoking it directly (see `ops/README.md`) |
| `ops/bot-ops.test.ts` | bun tests that spawn the real `ops/bot-ops.sh` (bash + jq from the host, a fake `docker` shim first on PATH that logs its argv, serves fixture files for the `exec … cat` reads, and runs the plugin-request write's `sh -c` for real against a scratch dir) against a throwaway config dir: `env-get`'s compose-style read of `.env` (last duplicate wins, `export`, CRLF, quotes, trimming) and `env-set`'s diff-then-validate order (issue #44) — an unchanged out-of-regex value never blocks, a changed invalid one still fails naming the key, differently-spelled-but-equal values are a no-op with no recreate, an `export`ed key is rewritten in place, a real change backs up + rewrites + recreates once; also pins the script's own `BOT_OPS_PROJECT`/`BOT_OPS_CONTAINER` checks (issue #41) — either one unset or outside the safe charset dies naming the problem, PROJECT checked before CONTAINER, in both cases before any docker call. Also (#101) the `PLUGINS`/`PLUGIN_INDEX_URL` whitelist forms and the manifest-merged plugin env keys (the fake `docker` shim serves a fixture `index.json`/`state.json` for the `exec … cat` reads): every index plugin's non-secret keys listed after the static ones in manifest order, whether or not the plugin is in `PLUGINS` (the `(#256)` describe), a secret key never listed by `env-get` (and, since #240, settable but write-only — covered by the `(#240)` describe blocks), diff-then-validate honoured for a changed plugin value, an unavailable index degrading to static-keys-only + stderr note, and `status.plugins`. Skips loudly when bash/jq are absent. Discovered by the root `bun test` like `ops/admin/server.test.ts` |
| `ops/install.sh` | Curl-able bootstrap for a fresh instance (`debug`/`prod`) with no checkout on the host — creates the config dir (`.env` from `.env.example`, never overwritten on re-run, `ADMIN_TOKEN` generated fresh into it), the shared `bin/bot-ops.sh`, the Dockge-managed stack dir's compose file, and that same stack dir's own `.env` (Compose's interpolation source, not the bot's config — see the gotcha) — the latter three always refreshed. Prints the `up -d --build` invocation, plus the optional `--profile admin` one. See `ops/README.md` |
| `ops/admin/` (`server.ts` + `public/index.html` + `Dockerfile`) | Small per-instance admin web panel — an authenticated wrapper around `ops/bot-ops.sh`'s operations (`status`/`logs`/`restart`/`env-get`/`env-set`/`env-schema`, `routing-get` since #240, and `plugin-request` since #105, which the four routing writes use too since #240), plus a few server-native routes that never shell out (`/api/whoami`, `/api/admins`, `/api/branches`, `/plugin-admin/<name>.js`, `/api/plugin-proxy/<name>`, and — #238 — the page's two stylesheets `/rb-theme.css` and `/admin.css`, public at this layer like the page itself). `server.ts`'s auth check, route→subcommand mapping, and request handler are pure/DI'd (`HandlerConfig` injects `runBotOps`, matching `updateReport.ts`'s injected-deliverer shape) so `server.test.ts` covers them without a real subprocess; only the `import.meta.main` block at the bottom does real I/O (spawns `bot-ops.sh`, binds the port) — and it **refuses to start** on two misconfigurations rather than degrading: no `ADMIN_TOKEN` (no door-2 auth), and a **relative `BOT_OPS_CONFIG_DIR`** (issue #60 — `resolveAdminsFile` is absolute-only, since the panel places `admins.json` from that value itself and so isn't covered by `bot-ops.sh`'s own guard; an *unset* value still starts, in bootstrap-only mode). `public/index.html` is the whole page — markup plus two inline scripts (the colour-scheme one in `<head>` and the page script), no build step at deploy — styled by the two committed stylesheets it links (`rb-theme.css`, `admin.css`; own rows below) and organised as three tabs, **Overview** (status, restart, logs), **Plugins** (the plugin list and plugin settings) and **Settings** (config, admins, identity), with the token gate and the out-of-date banner above them (#238). The open tab is kept in the URL hash (`#overview`, `#plugins`, `#settings`; written with `history.replaceState`, so switching adds no history entry); any other fragment — including a section id such as `#admins-section`, which used to scroll to that section — opens Overview. Its config form renders from whatever `GET /api/env` returns rather than a hardcoded field list, and its changes are applied from the one Apply bar (#257, below), which posts only the changed keys (pure `planEnvSave`, which `server.test.ts` lifts from the page source and pins — issue #44); the raw `PLUGINS` field is not rendered there (a `name@version` pin is set in `.env`). Its **Modify Plugins** section (#102) lists the Plugin Index alongside installed state and lets you add/remove a plugin by toggling `PLUGINS`: `GET /api/plugins` merges the index (fetched via the injected `listPluginIndex`, a `listBranches`-style on-demand `.env` read + fetch/cache) with `status`'s `plugins` and the current `PLUGINS` — pure `parsePluginIndex`/`mergePluginsView`, unit-tested — and degrades to a `200`+`indexError` (installed-only) when the index can't load; a tick or untick is applied from the Apply bar as the `PLUGINS` line of its one `POST /api/env` (pure `planPluginsSave` keeps `name@version` pins + manifest order, lifted and pinned like `planEnvSave`), never a runtime code install. **The Apply bar (#257)** collects every change that needs a restart — the plugin ticks and every edited config field — into one bar (`#apply-bar`, the last child of `<main>`, outside every tab panel, sticky at the bottom, `role="status"`) and sends them as a single `POST /api/env`, `PLUGINS` first: the pure `planApply` (`APPLY_PLAN`) composes `planPluginsSave` and `planEnvSave`, the pure `applyBarView` / `describeApplyFailure` / `failureWroteNothing` (`APPLY_VIEW`) decide its text and buttons and whether a failed apply re-reads the controls (#272), the DOM half (`APPLY`: `collectPending`, `refreshApplyBar`, `applyPending`, `discardPending`) is lifted and run against a stubbed page whose `querySelectorAll` throws on any selector but the collector's two (its one `querySelector` answers only the open-tab lookup that focus recovery uses), and `syncTagValue` (`TAG_SYNC`) is how a chip change reaches it; there is no Save button and no `confirm()` on apply. `server.test.ts` lifts the blocks the page marks `// NAME:begin` / `// NAME:end` (a test pins each is present exactly once): `TIMEOUT_SIGNAL`, `PLUGIN_ADMIN_HELPERS`, `PLUGIN_BADGES`, `PLUGIN_BADGE_CLASSES`, `PLUGIN_REQUEST_HELPERS`, `PLUGIN_REQUEST_SEND`, `PLUGINS_SAVE_PLAN`, `ENV_SCHEMA`, `ENV_SAVE_PLAN`, `APPLY_PLAN`, `APPLY_VIEW`, `APPLY`, `TAG_SYNC`, `OUTDATED_BANNER_HELPERS`, `RESTART`, `HAS_ACCESS_SESSION`, `TABS`, `TABS_DOM`, `LOGS_SCROLL`. **#105** adds the update-action buttons to each card with an available update — **Update now** / **Schedule** (a `datetime-local` → offset-bearing ISO via the pure, pinned `scheduleAtToIso`) / **Remind me in 7 days** / **Skip this version** / **Cancel scheduled update** — plus a "What changed" release-notes block and a compat note. Each POSTs the pure `pluginRequestPayload` (never carrying `requestedBy`) to the new server-native `POST /api/plugins/request`, which — Origin-guarded like `/api/admins`, then `parsePluginRequestInput`-validated (the panel-side mirror of the bot's `requests.ts` `validate`, incl. the anchored no-`/` version gate) — sets `requestedBy` from the **verified Access identity, never the body** (`email:<addr>` or `token`) and shells `bot-ops.sh plugin-request` with the JSON on stdin; `GET /api/plugins` now also surfaces each plugin's `scheduled`/`skippedVersion`/`remindAt` (the Cancel/marker inputs) and a `compatible`/`neededHostApi` computed against `HOST_API_VERSION` (a drift-pinned mirror of `contract.ts`). The DOM render (`buildPluginUpdateBlock`) stays browser-only like `buildPluginRow`; its pure inputs (`pluginUpdateBadge`, `compatible`, `scheduled`) and both boundaries (`parsePluginRequestInput`, the lifted `sendPluginRequest`) are the tested seams. **Admin tabs (#123, #165):** `GET /plugin-admin/<name>.js?v=` serves a plugin's own `dist/admin.js` same-origin (fetched server-side from `ADMIN_ASSET_HOST`, size-capped, public at this layer like the page — before the `/api/` auth gate) and `GET /api/plugin-proxy/<name>?path=&v=` proxies a data asset from that plugin's own package (authed, under `/api/`); both accept `?v=<installedVersion>` to pin delivery to the running code rather than the index's current release. `renderPluginAdmin`/`mountPluginAdmin` (client) import the bundle and mount it via `makeAdminApi`'s bridge (`getEnv`/`setEnv`/`getState`/`proxyFetch`), gated by `adminTabState`/`bundleMountDecision` (`PLUGIN_ADMIN_HELPERS`, lifted + pinned). **#242 routing routes:** `GET /api/routing` is a `buildInvocation` branch (`routing-get`); the four writes (`POST /api/routing`, `POST /api/webhooks`, `DELETE /api/webhooks/<channel id>`, `POST /api/discovery/refresh`) are server-native `plugin-request` actions — `parseRoutingSetInput` / `parseWebhookAddInput` rebuild the body, `queueRoutingRequest` adds `requestedBy` (verified identity) and a server-minted `id` and queues it on stdin, `redactWebhookUrls` cleans anything bot-ops wrote (a webhook URL never leaves the stdin payload). See `ops/README.md`'s Admin panel section for the two-door auth model |
| `ops/admin/admin-contract.ts` | The panel's own copy of the shape the plugins repo's `packages/api/admin.ts` defines — `ADMIN_API_VERSION` + the `AdminApi`/`SaveResult` types the server and `public/index.html`'s `makeAdminApi` need. Not `MountAdmin` (that type describes a plugin's own admin bundle, never imported by the panel) and, unlike `HOST_API_VERSION`'s cross-repo pin, deliberately **not** drift-tested against the plugins repo — a mismatch is safe-fail (the panel just refuses to mount), so this file's own header says to bump it in lockstep by hand instead. The admin-UI analog of `src/plugins/contract.ts`; kept in sync with the Dockerfile's `COPY` by the ratchet test (see the Gotchas entry), not by hand. See `docs/adr/0005` |
| `ops/admin/public/admin.css` | The panel's own layout and the few composites `@rackbops/styles` does not ship — the page column and header, the tab panels, the chip editor (`tag-field`), plugin rows and update block, the `msg` / `field-hint--danger` notices (a severity bar, never coloured small text), and the bare `label`/`input`/`select`/`button` a plugin's own settings tab builds (selectors wholly inside `:where(.plugin-admin-tab …)`, so zero specificity and a bundle's own styling still wins). Written with `--rb-*` tokens only — no colour, `font-family` or `border-radius` literal, and no custom property of its own — pinned by `server.test.ts`'s "admin.css uses tokens only". It restates `[hidden] { display: none !important }`: an author `display` rule (`.adm`, `.rb-alert`) beats the browser's own `hidden` rule, so without it the hidden app (behind the token gate), the hidden tab panels and the drift banner would all show |
| `ops/admin/public/rb-theme.css` | **Generated — never hand-edit.** The `arcane-obsidian` (dark) and `arcane-parchment` (light) bundles of the pinned `@rackbops/styles`, concatenated by `ops/admin/theme/build-theme.ts` and committed; line 1 names the version it came from. The page picks between the two from `prefers-color-scheme` (an inline script sets `data-rb-style` on `<html>` before the stylesheets load; there is no toggle). See the Gotchas entry for the bump procedure |
| `ops/admin/theme/` | Build-only package (`package.json` pinning `@rackbops/styles` exactly, `build-theme.ts`, its test, a README) that writes `../public/rb-theme.css`. Nothing in it runs at deploy or runtime, and the admin image never copies it (the Dockerfile's `COPY` lines name only `package.json`/`bun.lock`, `server.ts`, `admin-contract.ts` and `public/`). `composeThemeCss()` is the pure half; the `import.meta.main` block reads the pin, refuses anything but an exact `x.y.z`, refuses an installed version that differs from the pin, and writes the file |
| `src/storage.ts` | Shared atomic-JSON-file primitives — `writeJsonAtomic`/`readJsonOrFresh` (the same temp-file-then-rename + corrupt-file-moved-aside pattern `state.ts` established) plus two ways to serialize concurrent access: `createJsonWriter` (one file, whole-value writes) and `createKeyedJsonMutator` (many files, each identified by path, and — unlike `createJsonWriter` — serializes the READ that precedes the write too, not just the write itself, so two concurrent read-modify-writes to the same file can't lose one). `writeJsonAtomic` alone is still NOT safe for two callers *in the same process* racing the same path — a lost-update risk from the two writes themselves interleaving, which only `createJsonWriter`/`createKeyedJsonMutator` actually serialize; a caller reaches it directly only through one of those two wrappers. Its temp NAME, though, is per-process-unique since #154 (`${path}.${pid}.${counter}.tmp`, was a fixed `${path}.tmp`) — see the gotcha. Also exports `resolveDataDir(env)` and the `DATA_DIR` it produces — `state.ts`/`handoff.ts`/`index.ts` all **import** it rather than recomputing it (#139; three separate spellings of the same path is what let the suite write the developer's real `data/`). Defaults to `join(import.meta.dir, "..", "data")`; a `BOT_DATA_DIR` override relocates the whole directory, absolute-only, and under `bun test` an unset override is a hard error rather than a silent fall back to the checkout. **A test seam, not an operator knob** — deliberately absent from `.env.example` and from `bot-ops.sh`'s whitelist; see the gotcha. Handed to plugins as `HostApi.dataDir`. **Moved up from `src/warbandeer/` in #99** so it is the host's storage module — its four primitives ARE `HostApi.storage`, now consumed by plugins (the warbandeer plugin's links/characters storage) rather than any in-tree code. Never recompute a data path from `import.meta.dir` under `src/plugins/` — that is two hops from `data/`, not one |
| `src/plugins/contract.ts` | Types plus one integer constant (`HOST_API_VERSION`) — the host<->plugin contract (`HostApi`, `Plugin`, `PluginModule`, `PluginCommand`, `TickCheck`) and the Plugin Index / Plugin State / request shapes. No runtime code at all (pinned by `contract.test.ts`), so it can be read before the `Client` exists and vendored verbatim by the plugins repo. See `docs/adr/0004`. **#184:** `Plugin` gained an optional `dispose?(): Promise<void>` counterpart to `activate?()` — a type-only, backwards-compatible addition (no `HOST_API_VERSION` bump; `contract.test.ts`'s single-const pin is untouched), so an older plugin without it is simply never called |
| `src/plugins/contract.test.ts` | bun tests pinning `contract.ts`'s invariant three ways: stripped of types by `Bun.Transpiler`, the module is exactly `export const HOST_API_VERSION = 1;` (so no value import, re-export, dynamic import or stray statement survives); every `import` in the comment-stripped source is `import type` (and no `require`/`export … from`); and `HOST_API_VERSION` (= 1) is the only runtime export |
| `src/plugins/index.ts` | `loadPluginIndex(url, dataDir, deps?)` — fetches the Plugin Index with a `timeoutMs`-bounded `AbortSignal` (default 5s; a bare absolute-path `url` is rewritten to `file://` first, since `fetch()` can't resolve a schemeless string), validates its shape — including that every entry's `intents` are numbers and that no two entries share a `name`, since discord.js throws on a non-number intent and a duplicate name would silently collapse in `registry.ts`'s `Map` lookup — and on success atomically caches it to `data/plugins/index.json` wrapped as `{ writtenAt, index }` (`deps.now()` — deliberately distinct from the manifest's own `generatedAt`, which is the plugins repo CI's clock, not this bot's). A fetch-side failure (unreachable, timeout, non-200, bad JSON, bad shape) falls back to the cached copy with a `[plugins] index unreachable (…)` warning, or an empty index with a warning if there's no usable cache; a *cache-write* failure after an otherwise-good fetch is different — it keeps and returns the freshly-fetched data (`source: "fresh"`) with its own warning, since discarding good data over a volume write error would be worse than the write error itself. Either way — **never throws**, so a manifest outage degrades to "no new plugins," never a boot failure. `readFile`/`writeFile` default to real `Bun.file`/mkdir+temp-write+rename (the same shape as `src/storage.ts`, copied rather than imported — `src/plugins/*` stays dependency-free of `config.ts`) but are injectable for tests |
| `src/plugins/index.test.ts` | bun tests (real temp dir via `mkdtempSync`, only `fetch`/`now` faked) for the full fallback matrix — fresh writes an atomic `{writtenAt, index}` cache; a rejected/timed-out fetch, a non-200, invalid JSON, and an invalid shape all fall back to the cache with the `writtenAt`-bearing warning; no cache or a corrupt cache falls back to empty; a cache-write failure after a good fetch still returns `"fresh"` rather than discarding it |
| `src/plugins/registry.ts` | Pure selection, no I/O. `selectPlugins(index, configured, hostApiVersion, coreCommandNames, installed?)` walks `configured` (parsed `PLUGINS=` tokens) in order, skipping — with an exact reason string, kept for later reporting by #99/#101/#102 — a name absent from the index, a `hostApiVersion` mismatch, or a command that collides with a core command or an earlier-selected plugin's. **An older pinned/last-good version can be kept past an index host-API bump (#222):** the index's `hostApiVersion` describes `entry.version` alone (`PluginRelease` carries none), so the host-API check is no longer an unconditional skip. When the index's current version needs a NEWER host than the bot's, the plugin is kept if every version `installPlugins` may try is provably older than `entry.version` — the explicit `PLUGINS=name@version` pin and only that, otherwise #104's `targetVersion` and, should that install fail, the last-good `installedVersion` (the optional `installed` map, built from the previous `state.json` by the pure, shape-tolerant `pinsFromState(state)`) — and the entry declares no intents (they would be unioned into the whole `Client` for a version that isn't the one running). Everything else stays skipped: no pin, a version equal to / newer than / unorderable against the current one, a current needing an OLDER host than the bot's, an entry with intents. Not modelled: `installPlugins`' newest-cached fallback (it needs disk access). **#223: `install.ts`'s `reconcileManifest` checks a kept version's own declared `hostApiVersion` against this host's `HOST_API_VERSION` (not `entry.hostApiVersion`, which describes only the index's current version) on both the cache-reuse and post-extract paths, and refuses a mismatch** — a `commands`/`env` divergence only warns, and a missing/unparseable `package.json` never refuses. A skipped plugin still comes back in the result; `entry` is only absent for the "not in the plugin index" case, since every other skip reason still has a real `PluginIndexEntry` to attach. `collectIntents(core, selected)` unions `core` with every non-skipped plugin's declared intents, deduped, first-seen order — this is what lets `src/index.ts` size the discord.js `Client`'s intents to exactly what the selected plugins need, before any plugin code runs. `describeSkips(selected)` renders each skip as a `"<name>: <reason>"` line, kept as a pure function (rather than logging inline here) so the exact reason text stays unit-testable without a console spy — `src/index.ts` is what actually logs them |
| `src/plugins/registry.test.ts` | bun tests for `selectPlugins` (PLUGINS-order not index-order, each skip reason's exact string, a skipped plugin's commands never blocking a later plugin from the same name, the pinned-version pass-through; **#222:** an older explicit pin / last-good `installedVersion` / `targetVersion` kept past an index host-API bump; the incompatible current — and any equal, newer or unorderable version, and an entry that declares intents — still skipped; a target that can fall back to the last-good needing BOTH to be older; versions ordered numerically, not as text — down to the patch field, seeing past a prerelease tag on older numbers, and rejecting text that only looks like a version or an index `version` that isn't one; a current needing an OLDER host skipped whatever is pinned; a kept pin still hitting the name/command collision checks), `pinsFromState` (name-keyed pins, unusable entries dropped, any parsed shape tolerated without a throw) and `collectIntents` (dedup + first-seen order, a skipped plugin contributing no intents even when its entry declares some, no entry at all handled without throwing) |
| `src/plugins/install.ts` | Installs the plugins `selectPlugins` chose (#99): resolves `package@version` on the registry (`https://registry.npmjs.org`, or `PLUGIN_REGISTRY_URL` in dev), downloads the tarball, verifies `dist.integrity` (SHA-512 SRI) BEFORE extract, and extracts the bundle into a sibling staging dir (`<name>/.staging-<version>-<now>/`), reconciles it (below), then renames it into place as `data/plugins/<name>/<version>/dist/plugin.js` (+ `package.json`; `tar --strip-components=1` drops only the tarball's leading `package/`, so `plugin.js` keeps its `dist/` level) — **#224: extract-then-rename, never extract-straight-into-`versionDir`**, so anything at `<version>/dist/plugin.js` came from a completed extract and a `tar` that dies mid-write leaves nothing reusable; any earlier partial `versionDir` is cleared first, and the staging dir is always removed in a `finally` (a no-op once the rename has moved it). Version resolution: operator pin (`PLUGINS=name@version`) → **#104's transient `targetVersion`** (an explicit `/plugins update`) → previous `state.json` `installedVersion` → newest cached → index's current — the bot NEVER moves an installed plugin to a newer version on its own. A cached bundle is reused with no fetch. **#223: `reconcileManifest`** (exported, unit-tested directly) reconciles the extracted bundle's own `package.json` `botPlugin` block against the host on BOTH the cache-reuse fast path (before reuse) and a fresh extract (against the staged copy, before the rename commits it) — total, never throws: a missing/unreadable/unparseable `package.json` or one with no `botPlugin` block warns and installs; `commands`/`env` key-set divergence from the index entry warns; a `hostApiVersion` that differs from this host's `HOST_API_VERSION` (deliberately NOT `entry.hostApiVersion`, which describes only the index's current version — #222's `keepOlder` can legitimately install an older version built against a different host API) refuses the bundle, recorded in `skips`/`fallbacks` like any other install failure. **#104 failure fallback:** when a `targetVersion` install fails (including a `reconcileManifest` refusal), it reverts to the recorded `installedVersion` (deterministic — NOT `newestCachedVersion`, which could out-rank a hand-lowered pin), so the bot comes back running on the previous version; recorded in `InstallResult.fallbacks`. Any per-plugin failure with no usable fallback is recorded in `skips` and affects only that plugin. `newestCachedVersion`'s directory listing excludes a leading-`.` name (a crashed extract's `.staging-*` leftover) as well as `tmp`. Pure over injected `{ fetch, extract, now, log }`; `tarExtract` (production `extract`) shells `tar` (present in `oven/bun:1-slim`). No plugin code runs here |
| `src/plugins/install.test.ts` | bun tests: registry URL + encoded package/version, SRI verified (mismatch → skipped, nothing extracted), cache reused with no fetch, pin/previous-installed version wins over the index's newer one, 404 skipped with a reason while others install, selection-skipped plugins ignored; **#104: a broken `targetVersion` reverts to the recorded previous (never a newer cached one — the silent-upgrade guard), and no cached previous is a plain skip**. **#224: a part-way extract leaves nothing reusable and re-fetches next time, a leftover `.staging-*` dir is never treated as a cached version, the bundle is extracted to staging and moved into place (not written to `versionDir` directly), and an earlier partial `versionDir` is replaced, not merged.** **#223: a `hostApiVersion` mismatch is refused (and never lands) on both the fresh-extract and cached-reuse paths, with a reason naming both numbers; divergent `commands`/`env` warn but still install; a missing or unparseable `package.json` installs with a warning and is never refused; and a `keepOlder` (#222) bundle kept below the index's current `hostApiVersion` is not refused, since reconciliation compares against the host's `HOST_API_VERSION`, not the index entry's.** `tarExtract` real-`tar` case is `skipIf(win32)` (Windows `tar` mangles `C:\` drive-letter paths; the container is Linux) |
| `src/plugins/host.ts` | Loads/wires/activates the installed bundles, inside `activate()` after `takeOver()`. `createHostApi` builds a plugin's `HostApi` — `env` is ONLY the keys the entry declares (never the whole environment), `log` prefixes `[name] `. `loadPlugins` imports each bundle and runs `createPlugin(host)`, isolating a throwing import/createPlugin (recorded, skipped, never fatal). `pluginCommandMap` maps bare→command, dropping a whole plugin's set on any collision with core or an earlier plugin (defence in depth behind `selectPlugins`). `buildCommandBody(prefix, coreJson, map)` = core JSON then each plugin command built via `commandNamer(prefix)` (dropping a builder that produced the wrong name). `pluginTicks` wraps each plugin tick so it runs only while that plugin's `running` flag is set, and bounds the wait on each at `PLUGIN_TICK_TIMEOUT_MS` (30 s, #217): a tick that hasn't settled by then rejects, `runTick` logs it as `[tick:<plugin>:<tick>]` and the plugins behind it still run. The abandoned call itself keeps running — concurrently with whatever runs next, that plugin's other ticks included (cancelling needs an `AbortSignal` in `TickCheck.run`, a contract change) — so `pluginTicks` skips that tick, with a `[plugins] <plugin>:<tick>: its previous tick is still running` warning, until the call settles, instead of re-entering it: a merely slow tick recovers, one that never settles silences that tick (its siblings keep running) until the bot restarts. Because that tick's `withCritical` section closes at the bound, a restart request or a `SIGTERM` drain during the abandoned call does not wait for it (see `restart.ts`'s header). `activatePlugins` runs each `activate()` in order, isolating a throw. **#184:** `disposePlugins(loaded, log, timeoutMs)` is `activatePlugins`'s counterpart, called once on the way out (`shutdown.ts`'s drain) — disposes only plugins that are actually `running`, each under its own `PLUGIN_DISPOSE_TIMEOUT_MS`-bounded window (one wedged plugin can't block the rest), isolating a throw — sync, async, or a malformed non-function `dispose` — exactly like `activatePlugins` isolates a throwing `activate()`; always flips `running=false` for every plugin it touches, win or lose, so `pluginTicks`' gate stops using a resource that dispose either released or failed to release. `readPluginState`/`writePluginState`/`buildPluginStateFile` produce `data/plugins/state.json`, preserving the operator-bookkeeping fields (`notifiedVersion`/`skippedVersion`/`remindAt`/`scheduled`/`pendingReport`) from the previous file and **consuming #104's transient `targetVersion`** (success → it becomes `installedVersion` + is dropped; failure → `installedVersion` stays the previous, `error` names the failed target, `targetVersion` still dropped so a broken target never retries forever — threaded via `InstallResult.fallbacks`). `mutatePluginState` is the ONE race-safe runtime read-modify-write of `state.json` (a shared `createKeyedJsonMutator`), used by #103's update tick **and #104's `/plugins` actions + boot report-back** — the boot `writePluginState` is a one-time whole-file write ordered before any tick fires. All pure/DI'd |
| `src/plugins/host.test.ts` | bun tests: env slice (declared keys only) + log prefix; import/createPlugin isolation with names; command-map collision drop; body = core-then-plugins and wrong-name-builder drop; tick `running` gate; **#217:** `pluginTicks`' per-tick bound and in-flight guard — a never-settling tick is abandoned as a rejection that `runTick` logs while the plugins behind it still run; the timer armed for the bound is cleared once the tick settles (default `PLUGIN_TICK_TIMEOUT_MS`, pinned under `TICK_MS`); a rejecting or synchronously-throwing tick surfaces its own error and releases the guard; a plain-value `run()` still works; an abandoned tick's late rejection is not unhandled; a tick whose previous call is still pending is skipped with a warning, not re-entered, until that call settles, and the guard is per check; a plugin tick that overruns the bound lets a pending restart land while its call is still running (the documented exception); activate order + isolation + `running`/`error`; `buildPluginStateFile` content + bookkeeping preservation (incl. #104's `targetVersion` consumption: success promotes it to `installedVersion` + drops it, a fallback keeps the previous + sets `error` + drops it); a `read`/`write` `state.json` round-trip; **#222:** that same real file read back through `pinsFromState` → `selectPlugins` keeps a plugin selectable past an index host-API bump — including after a boot that *skipped* it (`buildPluginStateFile` preserves the last-good `installedVersion` unless an explicit `PLUGINS` pin overrides it) — and selection composed with the real `installPlugins` (a fetch that always 404s records what the installer reaches for): the exact versions for representative shapes, plus a grid of explicit-pin / `installedVersion` / `targetVersion` combinations proving that whenever selection keeps a plugin the installer, fallbacks included, reaches only for versions older than the incompatible current. **#184:** `disposePlugins` — disposes only `running` plugins and flips `running=false` for each (a not-running plugin's `dispose` is never called); a `running` plugin with no `dispose()` at all is skipped but still flipped (the wow-plugin case); a throwing (async or sync) `dispose` is isolated and logged, the next plugin still disposed; a never-resolving `dispose` is bounded by its own per-plugin timeout, still flips `running=false`, and doesn't block a LATER plugin's own dispose |
| `src/plugins/updates.ts` | Plugin update NOTIFICATIONS (#103) — tell admins once per new version, with what changed, and NEVER install/restart/move a pin (#104 does that). Pure: `compareSemver` (prerelease-aware, no dep), `decidePluginUpdates` (per installed plugin → `notify`/`remind`/`none` vs `notifiedVersion`/`skippedVersion`/`remindAt`; an incompatible newer version still surfaces), `releaseNotesBetween` (`(from,to]`, newest-first, clamped + URL tail), `notificationMessage` (compatible = the four options; incompatible = "needs a newer bot"), `renderPluginsList`. Impure orchestrator `checkPluginUpdates(deps)` — every side effect injected — re-fetches the index, decides, `deliverPluginNotification` (DM each admin, channel fallback once, no-admins→warn), and records `notifiedVersion`/`availableVersion` via `mutatePluginState` only after a successful delivery (capped retries). **#104 adds the acting layer** (still importing nothing from restart/install — both are injected deps): pure `parseScheduleTime` (`HH:MM`→next UTC occurrence / ISO-8601-with-offset / else error), `planPluginAction` (the `/plugins update|remind|skip|cancel` state mutation + reply, refusing no-newer/incompatible), `decidePluginReportOutcome` (boot report: success / reverted-still-on-previous / installed-but-failed-to-start); `checkPluginUpdates` now also runs a DUE `scheduled` update — heads-up DM, set `targetVersion`+`pendingReport`, clear `scheduled`, injected `requestRestart` — with the serialized `mutateState` closure as the sole arbiter, guarded by injected `restartPending` (one restart per tick). A racing `/plugins cancel` is safe either way: clearing `scheduled` first makes the arbiter no-op, and landing after the fire still clears the `targetVersion` the fire set (so the queued update can't apply — `cancel` clears both). `decidePluginUpdates` suppresses re-notifying a version that's already `scheduled`; and `reportPluginUpdateOutcome` clears `pendingReport` (via the shared mutator) BEFORE delivering DM→channel. **#105:** the per-action mutations are extracted into pure **version-parameterized builders** (`pinUpdateNow`/`scheduleUpdate`/`remindLater`/`skipVersion`/`cancelPending`) that `planPluginAction` calls with the index's latest and the request mailbox calls with the request's explicit pin; and `isDiscordUserId` guards the two DM sites (heads-up + report-back) so a panel-origin `requestedBy` (`email:…`/`token`) skips a futile `users.fetch` and logs the outcome instead |
| `src/plugins/updates.test.ts` | bun tests (pure, no env/Client): `compareSemver` incl. prerelease; `decidePluginUpdates` (not-newer/uninstalled→no decision; notify-once; notified/skipped/snoozed→none; snooze due→remind; incompatible→`compatible:false`); `releaseNotesBetween` range/order/clamp/placeholder; `notificationMessage` both texts; `renderPluginsList`; `deliverPluginNotification` (per-admin DM, single channel fallback, no-admins warn, all-fail); `checkPluginUpdates` (notify-once + persist, retry cap, remind clears `remindAt`, no-admins persists nothing). #104 tests: `parseScheduleTime` (UTC wrap, ISO+offset, rejection), `planPluginAction` (each action's mutation/reply + refusals), `decidePluginReportOutcome` (3 branches), `reportPluginUpdateOutcome` (clear-before-deliver + fallback), and the scheduled-execution property set — **no restart without a due schedule**, a due schedule restarts once (sets `targetVersion`/`pendingReport`, clears `scheduled`), deferral on `restartPending`, and the cancel-race arbiter (a racing cancel stops the restart) |
| `src/plugins/requests.ts` | The #105 plugin request **mailbox** consumer. `consumePluginRequests(deps)` drains `data/plugins/requests/*.json` (filename/epoch order) — the panel can't write `state.json`, so each panel action is a file the bot applies. Validates each against `PluginRequest` + formats (`validate`, exported/pure: action ∈ the 5, `plugin` `^[a-z][a-z0-9-]*$`, **`version` anchored semver with no `/` — the traversal gate**, `at` ISO-with-offset, `days` 1-999, plugin must be **installed**, and the one compat check the index allows: current-version-incompatible → reject); applies the survivors through #104's version-parameterized builders (honoring the request's EXPLICIT pin, not the index's current) via the shared `mutatePluginState`; deletes on success (ENOENT-tolerant); a bad file is moved to `requests/rejected/` (never thrown). **Single-flight** (a module promise chain) so the boot drain, a tick drain and the five-second timer's can't double-process. Collects a single restart-needed flag and `requestRestart`s ONCE after the whole drain (co-dropped `update-now`s share it). Pure over an injected FS seam + mutate/restart deps. **#241:** the four routing actions (`ROUTING_ACTIONS`) are dispatched to `src/routing/requests.ts`, given `deps.routing` (absent: rejected `routing is not available`); the Plugin Index and plugin state are loaded lazily, once per drain, only when an update action needs them (a drain of routing requests alone fetches nothing, and a load that fails leaves the update files queued and fails the drain only at its end, after the routing requests behind them were handled); `validate` runs inside a `try` (a throw is the rejection `validation threw — …`, #249) and echoes its untrusted values through `shown()`; a file that may carry a webhook URL (named `webhook-add`, its text holds one, or it parses as a `webhook-add`) is **deleted on rejection, never moved to `rejected/`**, and its reason carries no parser text (an update request that holds one is refused outright and deleted); a file that will not parse is read a second time after `tornReadRetryMs` (one wait per drain, paid by the first such file; a later one in the same drain is looked at again at once) before it is rejected (a writer older than #240 is not atomic); a request that was applied (routing or update), or any file that was refused, and cannot be deleted is remembered with the text read (an unreadable one as unread), not handled again, and its delete retried on each drain (an unreadable one is deleted again whenever it is still unreadable) — that memory is in-process only, so across a restart the loop an undeletable applied `update-now` would cause (restart, boot drain finds the file, pins, restarts…) is ended instead by `validate` refusing an `update-now` or `schedule` for the version already installed (`<plugin> is already on <version>`; `skip`/`remind`/`cancel` still apply), and the residual, an undeletable file AND an installed version that never becomes the requested one (a failed install, or a `PLUGINS=name@version` pin), is one restart per boot (#263); every reason goes through `redactWebhookUrls`; and a routing request that carried a valid `id` has its outcome written to `routing.json`'s `results`. Trust boundary: can only ever run the 5 update actions on an already-`PLUGINS=`-selected plugin, and the 4 routing actions against servers and channels the bot can see |
| `src/plugins/requests.test.ts` | bun tests (in-memory FS seam, fake mutate/restart): `validate` every accept + reject reason (unknown action, bad plugin/version, **`/` in version**, not-installed plugin, bad `at`/`days`, current-version-incompatible); the drain — filename order, delete-on-apply, honors the request's explicit version (not the index's), one restart across a multi-file drain, malformed → `rejected/` (never throws), **single-flight** (two concurrent drains apply each file once), empty dir a no-op. #241: the routing dispatch and its results, the lazy index load, a throwing validation not wedging the drain, `validate` on 100k-deep values, and secret-bearing files deleted rather than quarantined |
| `src/routing/model.ts` | **#237, ADR-0006.** The routing contract the bot chain, `ops/bot-ops.sh` and the panel all build against: the `RoutingFile` / `RoutingSecretsFile` / `DiscoveryFile` shapes, `ROUTING_VERSION`, `PLUGIN_NAME_RE`, `SNOWFLAKE_RE`, `freshRouting()` / `freshSecrets()` and `repairRouting()` / `repairSecrets()`. Pure; a repair never throws and always returns a new object. An untrusted key is tested against a regex before it is used, and unknown keys (a stray `url`, `__proto__`) are not carried over. The file's own `v` is not consulted (a hand-seeded file with no `v` keeps its placements); the result always says `v: 1`. **#241:** `RoutingFile` gains `results` (`RequestResult`, at most `MAX_RESULTS` = 20, newest last, keyed by an `id` matching `REQUEST_ID_RE`); `repairRouting` keeps only well-formed entries (a valid `id`, string `action` and `at`, boolean `ok`; `plugin` / `channelId` only in the shape of a plugin name / snowflake; `reason` clipped to 300 characters, well-formed via `clip`), and `withResult` appends and trims, replacing an earlier result under the same id. `droppedByRepair` (#260) is the report beside repair: given the same raw value it lists, as plain sentences naming the plugin, server or channel by its key (through `resolve.ts`'s `shown`; the one value shown is a bad `postTo`, clipped the same way), what `repairRouting` leaves out, including a file, `plugins` or `webhooks` that is not an object, and it says nothing for a webhook entry whose only fault is a stray `url` key (repair keeps the entry). A table-driven test keeps the two in step; only `readRouting` logs it. |
| `src/routing/resolve.ts` | **#237.** Pure routing decisions, no I/O and no discord.js: `isPlaced`, `hasPlacements`, `pluginsForGuild`, `announceTargets`, `commandAllowed` (a thread counts as its parent), `validatePluginRouting` (rejections worded for the panel to show). A plugin is always looked up with `Object.hasOwn`, never a bare `routing.plugins[name]` — `constructor` is a legal plugin name. `register.ts` (#239) uses `hasPlacements` and `pluginsForGuild`, and `requests.ts` (#241) `validatePluginRouting` and `shown` (the bounded, never-throwing way to put an untrusted value in a message); `post.ts` (#243) calls `announceTargets` and `gate.ts` (#243) calls `commandAllowed`. |
| `src/routing/store.ts` | **#237.** `readRouting` / `mutateRouting` / `readSecrets` / `mutateSecrets`, taking `dataDir` as a parameter and never reading `DATA_DIR`. Both files are read through `storage.ts`'s `readJsonOrFresh`; `routing.json` is written through one module-level `createKeyedJsonMutator`, so its read-modify-write is serialized. `mutate` always receives a repaired value, must return an object (anything else is refused before a byte is written, so a callback that forgot its `return` cannot erase the file), and what it returns is repaired again before it is written (a `url` key on a webhook cannot land in `routing.json`; text in the fields the shape allows, such as `broken`, is copied as given). `mutateSecrets` does NOT write through `writeJsonAtomic` (whose temp file gets the process default mode): it serializes its own read-modify-write per path, writes a randomly named temp file created exclusively with `0o600` and renames it into place, so the URLs are never in a file anyone but the owner can read (a secrets file put there by hand keeps its own mode until the first write replaces it), then calls `chmod` as a last step (a failure there is logged, not thrown). `live.ts` (#239) reads `routing.json` with `readRouting`; the request mailbox (#241) calls `mutateRouting`, `mutateSecrets` and `readSecrets` (through `livePluginRequestDeps`; a `webhook-remove` reads the secrets file to find a stored URL that routing.json no longer names), and nothing yet reads a URL back to post with. `readRouting` also says what the repair left out (#260): each `droppedByRepair` message, once per distinct message per process (`resetRoutingWarningsForTest` is the seam), as `[routing] routing.json: …; it is ignored` on `console.warn`; log output only (a logger that throws, or a report that throws, is swallowed), so what the bot acts on is still exactly `repairRouting(raw)`. Tests: `model.test.ts`, `resolve.test.ts`, `store.test.ts` — the tests that read real file modes skip on Windows and so run only on CI. |
| `src/routing/register.ts` | **#239.** Which slash commands go to which server, and the calls that put them there. `planRegistration` is pure. With no placed plugin it is `single`: today's one call — to the home guild, or with no home server to the global scope — carrying the full command body untouched. Otherwise it is `routed`: for every server the bot is in, the entries of the full body that are core commands or belong to a plugin living there (`ownerOf` finds the owner through the prefixed name), in the full body's order, plus `clearGlobal` when there is no home server. The body is built ONCE by `buildCommandBody`; a server's body is a filtered view of it, so a plugin's `build()` never runs per server. `registerPlan`: `single` is exactly one `put` and a failure throws (`index.ts` keeps its `catch`); `routed` is one `put` per server, sequentially, each failure recorded against its server as `<message> (<code>)` (clipped to 200 characters), then — when asked, and only once at least one server has taken its commands, since the global list is all any server has until then — an empty `put` to the global scope; it never throws. Tests: `register.test.ts`. |
| `src/routing/discovery.ts` | **#239.** `data/discovery.json`. `snapshotGuilds` is the only function here that touches discord.js: the text and announcement channels of every server the bot is in, by position then name, with `canSend` = View Channel and Send Messages (false when permissions cannot be computed), servers by name (a server whose name discord.js leaves unset, an outage stub, is listed under its id). `inviteUrl` is the README's invite with the bot's id. `describePlugins` lists the commands each loaded plugin registered (as registered, prefixed) and whether it `posts` (has ticks, read defensively like `pluginTicks`). `buildDiscovery` is pure — a server's `commands` is its registration or `null`, which with nothing placed is every server but the home one — and `writeDiscovery` writes atomically. `readDiscovery` (#241) reads it back for the bot's own validation of a panel request: null for a missing file, an unparseable one (moved aside) and anything without a `guilds` list. Tests: `discovery.test.ts`. |
| `src/routing/live.ts` | **#239.** The one stateful routing module. `initRouting` (once, from `activate()`), `applyRouting(reason)` (read routing → plan → register → write discovery; resolves `{ mode, results }`), `refreshDiscovery` (re-snapshot with the last registrations; a no-op before `initRouting`), and, since #259, `guildJoined(guild)` (routed mode: `applyRouting("joined <name>")`, so the new server gets its commands and appears in discovery; single mode: only a discovery refresh, except that a join of the home server — the one place single mode registers — runs the registration again; the mode is decided from a fresh read of `routing.json` OUTSIDE the chain, because `applyRouting` and `refreshDiscovery` each queue themselves and calling either from inside a queued job would wait on itself forever) and `guildLeft(guild)` (a discovery refresh; `routing.json` is never touched). Both do nothing before `initRouting` and never reject. Everything that registers or rewrites discovery runs on ONE promise chain, so a second call queues behind the first — the request mailbox (#241) calls `applyRouting` (after a `routing-set`) and `refreshDiscovery` (for a `discovery-refresh`, and once before refusing a request for something the file does not show) while a boot registration or a refresh may be in flight. In `single` mode a failure is rethrown once `discovery.json` is written (it is not, when the servers could not be read), so `index.ts`'s own `catch` and its operator message still run; it is written against the home server's entry when the bot can see that server, while a failure of the global scope (no home server) has no entry to go on and reaches the operator through that message only. `routed` mode never throws. A discovery write failure is logged and swallowed, and a read of the bot's servers that failed writes nothing (the panel keeps the last copy; a routed run registers nowhere). In `routed` mode it also warns, once per distinct message per process (`resetRoutingForTest` clears it), when `DISCORD_SERVER_ID` names a server the bot is not in and some loaded plugin that has commands is unplaced, since those commands are then registered nowhere (#260); nothing is said when the server list could not be read, when there is no home server, or when every plugin with commands is placed. Tests: `live.test.ts`. |
| `src/routing/requests.ts` | **#241.** The four routing actions of the request mailbox (`ROUTING_ACTIONS`): `routing-set` (one plugin's server map), `webhook-add`, `webhook-remove`, `discovery-refresh`. `parseRoutingRequest` is pure, shape only, and cannot throw on any JSON value (it never walks into one); its reasons name the field and never quote the value, and a malformed `id` is dropped, not fatal. `applyRoutingRequest(request, deps)` validates against `discovery.json` -- after ONE `refreshDiscovery()` and a re-check, so a server joined a minute ago is not refused for missing from an old file -- writes the validator's clean value (never the raw input) and stamps `updatedBy`; a `routing-set` then calls `applyRouting` (its rejection is logged and swallowed: the request is applied once `routing.json` is written). `webhook-add` matches `WEBHOOK_URL_RE`, asks Discord (`liveFetchWebhook`: `GET https://discord.com/api/v10/webhooks/<id>/<token>` built from the two captured groups, never the pasted string; 10 s; a redirect is an error; the fetch error is dropped and the body never returned beyond three ids), requires that it posts into a server and channel in discovery, then writes `routing.secrets.json` FIRST (the canonical URL) and `routing.json` second; `webhook-remove` is the reverse and clears whichever of routing.json and the secrets file (`readSecrets`) holds the channel, so a half-removed webhook can be removed again; a failed second write of an add puts the previous secret back. One webhook per channel: an add replaces the old one and clears `broken`. `RoutingRefusal` is an expected "no" whose reason is shown as it is. `redactWebhookUrls` is the belt behind the braces: every reason passes through it, and so does `requestedBy`, which lands in `routing.json` (a webhook-add's own token is also taken out of `requestedBy` and `id` wherever it sits). Tests: `requests.test.ts`, and `secrets-leak.test.ts`, the acceptance test that searches every file under a real data dir and every captured log line for every scenario's token. |
| `src/plugins/drain.ts` | **#241.** `startRequestDrain(opts)` -- the request mailbox drained every `REQUEST_DRAIN_MS` (5 s) on its own `setInterval`, so a panel change shows up while the operator is still looking; the 60-second `pluginRequests` tick check stays as the backstop. A beat does nothing when `!ready()` (the boot state write and boot drain have not landed), when `restartPending()`, or while the drain it started last is still running (a slow Discord lookup cannot queue a backlog); a drain that throws is logged and the next beat still drains. Deliberately outside the tick machinery. Started from `index.ts` right after `startScheduler(...)`, gated on plugin state (`isPluginStateReady`) and `restartPending`, each drain inside `withCritical` (so a restart an update-now asks for waits for it). Tests: `drain.test.ts` (through the `schedule` seam; the default is pinned through spies on `setInterval` / `clearInterval`, so no test waits on a wall clock). |

## Behavior

- **Dedup keys** in `BotState`: `seenReleaseIds` (`Record<owner/repo, number[]>`, each capped at 100).
  Restarts never re-announce. The WoW dedup keys (`dmfAnnouncedFor`, `weeklyAnnouncedFor`, `realmStatus`)
  were removed from `BotState` in #107 — the wow plugin owns them in its own `data/wow.json` now; any
  copies a prior version left in `state.json` are preserved untouched (the `{ ...raw }` load spread +
  `saveState`'s whole-object write), never read by the core again.
- **Realm watch** is the `wow` plugin's now (#107), not the core's: whenever Blizzard creds +
  `WOW_REALM` are configured it polls every 2 min and announces every UP↔DOWN transition. Its
  last-reading dedup lives in the plugin's own `data/wow.json` (seeded once from the legacy
  `state.json` copy), so the first observation seeds silently; a restart reuses the stored status, so it never *repeats*
  an already-announced transition but does announce a real one that occurred while the bot was offline.
  `ops/bot-ops.sh status` reads `wow.json` first, falling back to `state.json`, for the `realmStatus`
  field the panel + `wow-companion` consume.
- **Release polling** runs on a flat 15-min cadence plus a startup catch-up (there is no release
  cron in this fork — same shape as the self-update / plugin-update checks). Each repo in `config.watchedRepos` is polled
  independently; a repo's first-ever poll (its key absent from `seenReleaseIds`) seeds silently.
- **Command registration** (#239) decides at boot whether routing exists. `applyRouting` reads
  `data/routing.json` (with `readRouting`: a missing or wrong-shaped file is fresh, an unparseable
  one is moved aside and read as fresh) and hands it to the pure `planRegistration`. If it places no plugin — no file, an empty one, or
  one holding only webhook metadata — the plan is `single`: the same one PUT the bot always made (to
  `DISCORD_SERVER_ID`'s guild, or globally when that is unset) and the same `Registered N slash
  commands` line. If it places at least one plugin the plan is `routed`: one PUT per server the bot is
  in, one server at a time, in server-name order — the core commands plus the commands of every plugin
  that lives there — then one `Registered commands in N servers (...)` line, plus a `[routing]
  couldn't register commands in <name> (<id>): <error>` line per refusal. A refusal in one server
  (Discord's 50001, no `applications.commands` scope) is recorded against it in `discovery.json` and
  stops neither the other servers nor the boot; in `single` mode the failure still reaches
  `index.ts`'s long `catch`. With no `DISCORD_SERVER_ID` in routed mode the global command list is
  emptied after the servers, or every command would show twice — but only when at least one server
  took its commands: if none did, or the servers could not be read, it is left as it was (it is all any
  server has) and, when servers refused, the log says so. "At least one" is deliberate: if every server
  had to succeed, one that refuses for good (50001) would keep the global list, and every command
  would show twice, on every boot. The price is that a server that failed for a passing reason while
  others succeeded has no commands until the next registration (the next boot, a routing change from
  the panel, or a server the bot joins); it is in the log and in `discovery.json` with
  its error. `data/discovery.json` is written after every registration whose server list could be
  read — the boot one included, routing or not — and refreshed every 15 minutes by the `discovery`
  tick; a failure to write it is logged, never thrown. A server the bot **joins** after boot (#259,
  discord.js `guildCreate`) runs `applyRouting("joined <name>")` in routed mode — the whole registration
  again, which also rewrites `discovery.json` (the file the panel reads), so it lists the server within
  seconds — and, in single mode, only a discovery refresh: single mode registers to one place (the home
  server, or globally when there is none), which a join does not change, except that a join **of the home
  server** runs the registration again (a bot that was not in it at boot, so that registration was refused,
  and is invited later, or that was removed from it and is added back, has nothing there until it does;
  Discord is understood to drop a server's commands when the bot leaves it, which is not verified against
  a live server. Re-authorizing a bot that is still a member, the usual cure for a 50001, emits no join at
  all and waits for the next registration). With a home server set, any other joined server gets no
  commands in single mode, as before; with none, the global list already reaches it. A server it
  **leaves** (`guildDelete`) refreshes discovery and nothing else: `routing.json` is not touched, so being
  kicked and re-invited loses no placement (a server that is in routing but not in discovery is one the
  panel has to be able to show as unavailable; that view is #246's, and its issue text does not yet list
  this case). Neither does anything before `initRouting`.
- **The request mailbox** (#105, #241) is `data/plugins/requests/*.json`, drained in filename order by
  one single-flight consumer: at boot (after `initRouting`, before `markPluginStateReady()`), on every
  60-second tick, and every five seconds on `startRequestDrain`'s timer once the boot has landed. Nine
  actions: the five update ones on an installed plugin, and `routing-set`, `webhook-add`,
  `webhook-remove` and `discovery-refresh` against servers and channels the bot can see; nothing here
  enables a plugin. A routing request may carry an `id` (8-64 of `A-Za-z0-9_-`) that the panel chose,
  and once it is applied or refused its outcome is appended to `routing.json`'s `results` under that id
  (a request with no valid id records nothing). The Plugin Index is fetched only when a drain holds a
  file that parses, is not a routing request, and holds nothing that looks like a webhook URL (one that
  does is refused before the index is needed); if it cannot be loaded those files stay queued and the
  drain fails, as it always has, but the routing requests behind them are still handled and a file that
  will not parse is still rejected. A file that will not parse is looked at a second time (once, after
  `tornReadRetryMs`, 250 ms) before it is rejected, because a writer older than #240 writes with a
  non-atomic `cat > file` (an instance can run this bot with that older `ops/bot-ops.sh` until
  `install.sh` is re-run; it can only write the five update actions, so the half-written file is never a
  `webhook-add`) and the timer makes reading a half-written request likely enough to matter. That wait
  is paid once per drain, by the first such file: a later unparseable file in the same drain is looked at
  again at once
  (waiting for each would stall a mailbox of broken files), so the retry covers a write that finishes
  within the wait, for the first such file, and is best-effort for any later one. A request that
  was applied (a routing request or an update request), or any file that was refused, and cannot be
  deleted is remembered, with the text read (or as unread), and is not handled again: each drain only
  retries the delete, and a different text under the same name is handled as the new request it is
  (before #263 the update actions were not covered: an applied update request whose file could not be
  deleted was applied again on every drain, and an `update-now` asked for a restart each time). The
  memory is in-process only: `PluginStateFile` lives in `contract.ts`, which another repo vendors
  verbatim, and has nowhere to keep a list. So across a restart the loop is ended a different way:
  `validate` refuses an `update-now` or `schedule` for the version that is already installed
  (`<plugin> is already on <version>`, placed after the version, schedule-time and days checks so a
  malformed version is still "bad version"), and `skip`, `remind` and `cancel` for the installed version
  still apply. What that leaves: an undeletable file AND an installed version that never becomes the
  requested one (the install fails and falls back, is skipped at selection, or an operator's
  `PLUGINS=name@version` pin wins over the request) is one restart per boot, as it was. Likewise
  unchanged: an undeletable `update-now` still in the mailbox after a later upgrade past its version is
  replayed once against the newer install (it pins the older version and restarts, and is refused once
  that lands), and a stale `schedule` is applied again after a restart.
- **Self-update** asks whether the baked-in `GIT_SHA` **contains** the newest `BOT_BRANCH` (default
  `main`) commit on `GITHUB_REPO` (flat 15-min cadence + startup, only when `AUTO_UPDATE=true`;
  `/update` checks on demand with `force`). Stale → persist `attemptedUpdateToSha`, then exit 75
  for the orchestrator to respawn. `/update` is gated on the `ADMIN_USER_IDS` allowlist and fails
  closed when empty.
- **Update follow-up** closes the loop `/update` used to leave open: the command also persists a
  `pendingUpdateReport` (requester + both shas), and the next boot messages that requester with
  the build it actually came back on — `updated` / `noop` / `unexpected` named explicitly, since
  the no-op is otherwise indistinguishable from success. Only a `/update`-initiated restart leaves
  a report, so `AUTO_UPDATE` exits and host reboots stay silent.
- **Character linking** is now the `@rackbops/plugin-warbandeer` plugin (issue #3, extracted in #100),
  installed with `PLUGINS=warbandeer` and off unless `WARBANDEER_INGEST_PORT` is set. When on: `/link`
  → the desktop app redeems the code for a Device Token → every later character push replaces that
  Account Label's prior Character Snapshot wholesale (never merged). The `/link`/`/unlink` commands,
  reply strings, wire contract, and `data/links.json` + `data/characters/` are identical to the old
  baked-in connector. See `docs/adr/0001`–`0003` for why it's shaped this way.
- **Plugins** (`PLUGINS=`, #99): at boot, before the `Client`, the Plugin Index is fetched and
  `selectPlugins` picks/validates the named plugins (intents unioned onto the Client; a plugin whose index entry needs a different host API is skipped — unless it is pinned, or last-good in the previous `state.json`, on a provably older version and the entry needs a NEWER host and declares no intents, in which case that older version is kept, #222). Then inside
  `activate()` (after `takeOver()`) each selected plugin is installed from npm (integrity-verified,
  cached under `data/plugins/<name>/<version>/`, the installed version pinned — never auto-upgraded),
  `import()`ed, `createPlugin(host)`'d, its commands merged into the registration body, its ticks
  gathered for the scheduler, and `activate()` run — each step isolated so a bad plugin is skipped,
  never fatal. `data/plugins/state.json` records the outcome per plugin. An unset `PLUGINS` is a
  complete no-op: `Registered 3 slash commands` (the core set is `/report`, `/update`, `/plugins`, after
  #100 moved the connector's `/link`+`/unlink` into the warbandeer plugin and #107 moved
  `/dmf`/`/reset`/`/status`/`/transmog` into the wow plugin) — `PLUGINS=warbandeer` adds
  `/link`+`/unlink` back from the published plugin.
- **Plugin updates** (#103): *the operator's copy changes only when the operator says so.* A 15-min
  scheduler tick (`pluginUpdates`, with a startup catch-up like releases) re-fetches the Plugin Index
  and, for each installed plugin with a strictly-newer version, DMs the admins **once** — with the
  release notes between installed and available, and the choices `/plugins update|remind|skip` (or the
  panel). The **notification** never installs, restarts, or moves a pin — it only records
  `notifiedVersion` in `state.json` after a successful DM (channel fallback once, then capped retries).
  An incompatible newer version is surfaced worded "needs a newer bot", never offered for install. The
  tick is gated (`markPluginStateReady`) until the boot `state.json` write lands, so its keyed-mutator
  persist can't race that one-time whole-file write. `/plugins list` (admin-only, ephemeral) shows
  installed vs available + the first release note per plugin (and a pending schedule, if any).
- **Where a plugin posts** (#243). `HostApi.announce(message)` keeps its signature; it is `postForPlugin(<the plugin's name>, message, postDeps)`, with `postDeps` built once in `activate()`. It reads `routing.json` each time (no cache), asks `announceTargets` for the plugin's channels — every `postTo` it has, de-duplicated, in server-id order; none means `ANNOUNCE_CHANNEL_ID` — and posts to each in that order. A channel whose webhook is in `routing.json`, is not marked `broken` and has a URL in `routing.secrets.json` is posted through it; any other channel, and any webhook that failed, is posted to as the bot (`announceTo`, its `[announce]` line unchanged, and the webhook path prints the same one). A 401 or 404 marks the webhook `broken` (with the reason) and it is skipped from then on until it is added again; a 429, a 5xx, a timeout or an over-long message falls back to the bot for that post and marks nothing. With no routing, or none for the plugin, that is one post to `ANNOUNCE_CHANNEL_ID` — through that channel's webhook if it has a usable one, and as the bot otherwise, which is all it ever was before. Core's own release announcements do not go through this.
- **The channel gate** (#243). A plugin's chat-input command is put to `gateCommand` by `handleCommand` (`index.ts` hands it the plugin that owns the command, `whereOf` as a thunk, and `readRouting`); core commands never reach it, and buttons and modals are not gated, since they belong to a message that is already in an allowed channel. When the plugin's routing for that server lists channels, the command runs in them and in a thread whose parent is listed; anywhere else the reply is ephemeral and names the channels (`<#id>` is a channel mention, so Discord shows the name and links it). No entry for the server, `"all"`, a DM and a plugin with no entry all allow. The channel's own id decides first, and `whereOf` — which fetches the channel from Discord when it is not cached — is called only when that id is not listed, so a plugin with no routing, a listed channel, a DM and a core command cost no fetch (a DM or a core command does not even read routing). The gate fails open, and says so: it logs a `[gate]` line when it lets through a command it could not judge (the channel could not be looked at, so its parent is unknown) and when the gate itself fails (`readRouting` rejects; for a file it cannot parse it does not, it reads it as fresh, which allows everything, and logs that in its own `[routing]` line).
- **Acting on an update** (#104): `/plugins update <name>` writes a transient `targetVersion` and
  restarts into it now; `/plugins update <name> at:<HH:MM UTC | ISO-8601-with-offset>` stores a
  `scheduled` entry that the **same `pluginUpdates` tick** runs when due (heads-up DM, then a restart).
  `remind`/`skip`/`cancel` set `remindAt` / `skippedVersion` / clear the pending update. On the boot
  after an update the bot DMs the requester the outcome (✅ now on X / reverted-still-on-previous /
  installed-but-failed-to-start), clearing `pendingReport` before delivery. A broken target reverts to
  the previous version (its cached bundle) — never a silent upgrade. Guards: the write subcommands are refused until
  `markPluginStateReady` so they can't race the boot write; a due schedule defers while a restart is
  already pending, and its serialized mutate is the sole arbiter so a racing `cancel` (which clears
  both `scheduled` and `targetVersion`) can't leave a queued update behind.

## Gotchas

- **`BOT_DATA_DIR` is a test seam and a production footgun (#139).** It relocates the whole data
  directory, which is what stops `bun test` reading and writing the developer's real
  `data/state.json` — the suite used to destroy `attemptedUpdateToSha` and `pendingUpdateReport` on
  every run, and `update.test.ts` had an `afterEach` that wrote the real file *on purpose* to
  mitigate it. In a deployment it is dangerous: `buildCreateSpec` copies the container's env onto
  the replacement but derives `Binds` from the mounts, so a value pointing off the named volume
  means the original and the replacement write **different filesystems** — the replacement's
  `handoff.json` is never seen, every `/update` waits out `HANDOFF_DEADLINE_MS` and reports a
  replacement that "never reported in", and self-update is broken with nothing naming the cause.
  It breaks the ops surface too: `bot-ops.sh` hardcodes `/app/data/...` in six places, so `status`'s
  realm and plugin reads and `plugin-request` delivery would all be looking at the wrong directory.
  Mitigated by keeping it out of `.env.example` and `bot-ops.sh`'s `ALLOWED`, and by `index.ts`
  logging the resolved path on every boot — that log is what turns the symptom into a one-line
  diagnosis, and `index.test.ts` pins it. (Absolute-only is a *separate* guard against a relative
  value resolving against cwd back into a checkout; it does **not** help here, since the dangerous
  values are absolute.) The `bunfig.toml` preload is what sets it for tests, and it **must** be a
  preload: Bun runs every test file in one process with one module registry, so per-file
  `process.env` priming is first-importer-wins and a single new unprimed file sorting first would
  silently revert the whole protection. Note the preload is rooted at the repo — `bunfig.toml` is
  not walked up to, so `ops/admin`'s own package does not get it. Harmless today (no `ops/**` test
  imports `src/`), and the `NODE_ENV=test` refusal makes it fail loudly rather than corrupt if one
  ever does — but **run the suite from the repo root**.
- **A promise that never settles does not fail a Bun test on Windows: it hangs the run (#259).** With
  nothing else pending, Bun 1.3.14 on Windows neither applies the per-test timeout nor exits — a control
  with a live timer beside it fails cleanly at 5 s, the lone never-settling promise does not — so a
  deadlock (the bug the test is there to catch) turns into a stuck process rather than a red test, and a
  mutation run that reintroduces it hangs the runner. Any test that could deadlock races its await against
  a deadline (`withinDeadline` in `live.test.ts`) so it fails by name, and a scripted run of the suite
  passes `--timeout` and its own spawn timeout as well.
- **Plugin ticks are `running`-gated, and `activatePlugins()` runs BEFORE `startScheduler()`.**
  `activate()` awaits `activatePlugins()` (`index.ts:144`) — which flips each `LoadedPlugin.running`
  true once that plugin's `activate()` has succeeded — and only *then* calls `startScheduler(client,
  pluginTicks(loaded))` (`:163`). So the scheduler's first (synchronous) tick already sees
  `running:true` and fires each plugin's ticks at boot, preserving the boot-time announcements the
  core WoW checks used to make on that first tick (the wow plugin owns them now, #107). `pluginTicks`
  still wraps each tick to run only while `running` is true, kept as defence in depth (a tick that
  somehow fired before activate is skipped, not crashed). Don't reorder these so the scheduler starts
  first — that reintroduces the boot-announcement gap this ordering closes; `index.test.ts` pins it.
- **Plugin extraction shells `tar`, which is present in `oven/bun:1-slim`** (verified — the
  Acceptance runs `docker run --rm oven/bun:1-slim tar --version`). `src/plugins/install.ts`'s
  `tarExtract` runs `tar -xzf … --strip-components=1 package/dist/plugin.js package/package.json`.
  The real-`tar` unit test is `skipIf(win32)` because Windows `tar` reads a `C:\` drive-letter path
  as a remote host; the container is Linux, so production is unaffected.
- **A plugin's bundled `discord.js` must resolve to the host's single `/app/node_modules` copy**, or
  `instanceof`/builder identity breaks. Bundles are built `--external discord.js`, so the `import`
  stays bare and resolves by walking up from `data/plugins/<name>/<version>/dist/` to `/app/node_modules`
  — there must be exactly one copy on the running instance. (D1; the review Acceptance pastes
  `import.meta.resolve("discord.js")` from inside the container.)
- **Fork gotchas.** This repo was extracted, with history, from `nazumods/wow`'s
  `apps/warbandeer-discord` — see the top-of-file note for full credit to Nazuraki.
  `GITHUB_REPO` now defaults to this fork (`Rackbops/rackbops-discord-bot`), and self-update no
  longer path-filters (`BOT_PATH` is gone from `src/update.ts` / `src/redeploy.ts` — the whole
  repo is the bot now, so `fetchLatestBotSha` compares against the newest commit on the branch,
  full stop, and `buildRemote` builds from the repo root with no subdirectory). Remaining open
  work from the fork extraction:
  - ~~Self-update's rebuild needs `GITHUB_REPO` to be publicly clonable~~ — **resolved**: this
    repo is public (`Rackbops/rackbops-discord-bot`), so the daemon's remote git build context
    (no credential support) already works, for both self-update and the no-clone deploy model
    below.
  - `ops/bot-ops.sh`'s consumers — `apps/warbandeer-desktop`'s and `wow-companion`'s **Ops**
    tabs, plus the shared `apps/bot-ops` backend — live in the original monorepo and
    `roshne/wow-companion`, not here. The script itself still works over a direct SSH
    invocation (see `ops/README.md`'s "Run directly on the box" example) — but a caller now
    must also pass `BOT_OPS_CONFIG_DIR` + `BOT_OPS_COMPOSE_FILE` (see the config-dir gotcha
    below), which neither app's `ops.json` has a field for yet. Tracked as
    [roshne/wow-companion#197](https://github.com/roshne/wow-companion/issues/197) (the design
    doc's Q4: `opsCmd`/`configDir` fields).
  - ~~No CI~~ — **resolved**: `.github/workflows/ci.yml` now runs `bun run check` + `bun test`
    on every pull request (`on: pull_request` — not a direct push to `main`), a fork-native
    workflow rather than a carry-over of the original monorepo's path-scoped
    `discord-bot-test.yml`.
- **Config dir and compose file are two independent, required locations — never derived from
  each other or from a checkout.** `/opt/rackbops-discord-bot/<instance>/` holds `.env` +
  `backups/` (operator-precious, never auto-migrated — see `ops/README.md`); `.env` is never
  edited by anything but `ops/bot-ops.sh env-set` or the operator by hand. The **compose file**
  lives separately, at `/opt/stacks/rackbops-discord-bot-<instance>/docker-compose.yml`, purely
  so Dockge (nucbox's stack manager) lists and manages it — Dockge only recognises a compose
  file at a path under its own `DOCKGE_STACKS_DIR`, and doesn't care where anything that file
  references lives. `ops/bot-ops.sh` takes both as required env vars
  (`BOT_OPS_CONFIG_DIR`/`BOT_OPS_COMPOSE_FILE`), along with `BOT_OPS_PROJECT`/`BOT_OPS_CONTAINER`
  (issue #41 — a monorepo-era default on these last two once let a var-less invocation silently
  target a project/container no real deploy produces), with no fallback to its own script location
  or to any built-in name. **Both paths must be absolute; a relative one is rejected before docker
  is touched** (issue #60) — a relative path resolves against the invoking cwd, so hand-running the
  script from a checkout would point `env-set` at the *checkout's* `.env` and write
  `backups/.env.bak.*` (a live token) beside it, next to the `admins.json` `.gitignore` doesn't
  cover. `src/storage.ts`'s `resolveDataDir` cites this as the absolute-only precedent for
  `BOT_DATA_DIR`. `ops/install.sh` bootstraps a fresh instance's layout; see `ops/README.md` for
  the full runbook.
- **`docker-compose.yml`'s interpolation vars have TWO sources, in Compose's own precedence
  order: shell-exported values first, then a `.env` file in the stack directory** (Compose loads
  one automatically for `${VAR}` substitution — a mechanism entirely separate from the `env_file:`
  directive, which loads secrets *into* the container). `ops/install.sh` writes that second source
  itself: `/opt/stacks/rackbops-discord-bot-<instance>/.env`, holding this instance's real
  `BOT_ENV_FILE`/`BOT_OPS_CONTAINER`/`BOT_OPS_PROJECT`/`BOT_OPS_CONFIG_DIR`/`BOT_OPS_COMPOSE_FILE`/
  `BOT_BUILD_CONTEXT`/`GIT_SHA` — generated and refreshed on every re-run exactly like the compose
  file itself, never hand-edited. Its whole purpose is Dockge: Dockge's own Start/Stop/Restart
  buttons run `docker compose` with **none** of `install.sh`'s printed shell prefix, so before this
  file existed those buttons (and any bare `docker compose` typed by hand) fell straight through to
  `docker-compose.yml`'s own `:-` fallbacks — `env_file` pointing at a `.env` that was never there,
  `container_name` defaulting to the monorepo-era `warbandeer-discord` (issue #41). Shell-exported
  values still win when present, so `ops/bot-ops.sh`'s own explicit exports (`cmd_restart`,
  `cmd_env_set`) are unchanged — those still set `BOT_ENV_FILE`/`BOT_OPS_CONTAINER` per-invocation.
  `ops/install.sh`'s OWN printed step-2 command, by contrast, stopped doing this in #169: it used
  to shell-export `GIT_SHA`/`BOT_ENV_FILE`/`BOT_BUILD_CONTEXT`/`BOT_OPS_CONTAINER` too, redundantly
  with what the stack `.env` this bullet describes already carries — dropped once `install.sh`
  started writing that file itself, so the printed command now relies on the same auto-load
  Dockge's buttons always have (proven for an absolute `-f` path + an unrelated cwd, not just
  Dockge's cwd-equals-stack-dir case, in `ops/docker-compose.test.ts`). `docker-compose.yml`'s own
  `:-.`/`:-warbandeer-discord`/`:-.env` fallbacks are left in place on
  purpose — a plain `docker compose up -d --build` run directly from this checkout for local dev
  (documented in the compose file's own header) has no stack-directory `.env` to fall back to, and
  relies on exactly those defaults. Self-update's own rebuilds (`src/redeploy.ts`) were already
  remote-context-based before any of this, via the Docker Engine API directly, not `docker
  compose` — untouched by it.
- **Config lives in TWO files by design, a recorded deviation from the personal `CLAUDE.md`'s
  one-answer-file rule** (issue #169): the generated stack `.env` above (params) and the
  hand-edited `$CONFIG_DIR/.env` (secrets) are never merged into one — see `ops/README.md`'s
  "Why two files, not one answer file" and the repo's own `CLAUDE.md` for the reasoning.
- **A profile-gated service still gets fully interpolated and validated at `docker compose
  config` time, regardless of which `--profile` is active.** Profiles only filter which services
  actually get *created/started*; the whole file is parsed and every service's variables
  resolved up front. Verified live: an earlier draft of the `admin` service used
  `${BOT_OPS_CONFIG_DIR:?required}` for its required vars, and a bare `docker compose up -d
  --build` for **the bot service alone** — no `--profile admin` anywhere — failed immediately on
  that error, since `admin`'s definition (unset vars and all) is still part of the same parse.
  The fix: a plain `${VAR:-}` default on `admin`'s own vars, with the *volume mount sources*
  falling back to an obviously-fake path (`/nonexistent-set-BOT_OPS_CONFIG_DIR`) instead of an
  empty string, so a misconfigured `--profile admin` run still fails clearly — from Docker
  itself at container-creation time, once `admin` is actually the profile being brought up —
  rather than breaking every other profile's `config`/`up` in the same file.
- **`ops/install.sh` resolves a `DEPLOY_UID`/`DEPLOY_GID` once, up front, rather than trusting
  `id -u`/`id -g` at each chown site.** `curl | bash -s -- prod` (the normal flow) runs entirely
  as the invoking non-root user — `sudo` is only invoked *internally*, by `bootstrap_dir`, for the
  one-time `/opt` escalation. But `curl | sudo bash -s -- prod` (piping the whole interpreter
  through `sudo`) makes `id -u` report 0 for the *entire* script, not just that one escalation, so
  every file it creates — not only what `bootstrap_dir` chowns — would otherwise end up
  root-owned. `SUDO_UID`/`SUDO_GID` (set by `sudo` itself, never the operator) name who actually
  invoked it; with no `sudo` in the picture at all there's no real user to hand ownership to, so
  install.sh refuses outright rather than deploying an instance only root can maintain (issue
  #54). Every chown site (`bootstrap_dir`, `fetch()`, the `STACK_ENV_TMP` write, `backups/`) uses
  the resolved `$DEPLOY_UID:$DEPLOY_GID` unconditionally — in the normal non-root path this just
  equals the current user's own ids, and chowning a file to its own owner is always a safe no-op.
- **`cloudflared`'s `TUNNEL_TOKEN` is sourced the same way `admin`'s secrets are — named-var
  Compose interpolation, exported by hand out of `$CONFIG_DIR/.env` — not `env_file:`.**
  `env_file: ${BOT_ENV_FILE:-.env}` would load *every* secret in the instance's `.env`
  (`DISCORD_TOKEN`, `BLIZZARD_CLIENT_SECRET`, `GITHUB_TOKEN`) into the `cloudflared` container's
  process environment wholesale, and would still need an entrypoint rewrite to rename
  `CLOUDFLARE_TUNNEL_TOKEN` to the `TUNNEL_TOKEN` name the `cloudflared` binary itself expects —
  the same anti-pattern the `admin` service's own comment already rejects for itself.
  `ops/install.sh`'s printed next-steps read the value out of `.env` with the same
  `grep '^KEY=' ... | cut -d= -f2-` one-liner already used for `ADMIN_TOKEN` (issue #54).
- **The redeploy order is inverted on purpose (nazumods/wow#879), and the reason is a kernel boundary.** Every
  "shut down, then have something bring the replacement up" variant dies on the same thing: a
  detached process (`setsid`, double fork, `Bun.spawn({ detached: true })`) leaves its *parent*, not
  the container's **PID namespace** or **cgroup**, and per `pid_namespaces(7)` the kernel `SIGKILL`s
  every process in a namespace when its PID 1 exits. `init: true` doesn't help either — tini exits
  with its main child. Starting the replacement *first* means there is no window to survive, so
  nothing has to. Don't "simplify" this back into an exit-then-respawn.
- **The old container is removed, never just stopped.** Under `restart: unless-stopped` an exited
  container is respawned **on its old image** — the original nazumods/wow#868 failure, and now with two bots
  live. An explicit `docker stop` is exempt from that policy, which is why retirement is a daemon
  call rather than a process exit.
- **`GIT_SHA` is stripped from the replacement's copied env** (`buildCreateSpec`). Docker's
  `Config.Env` merges the image's baked ENV with the container's own, so copying it verbatim pins
  the *old* sha onto the new container and overrides the new image's — a bot that updated correctly
  would then report its own update as a ⚠️ no-op.
- **A plain `compose up -d` after a swap recreates the container — expected, not a bug.** The
  replacement copies the original's labels verbatim, including `com.docker.compose.config-hash`,
  which embeds the `GIT_SHA` build-arg environment of whatever `up` created the original — so
  compose sees a stale hash and recreates. Benign, verified on the box: compose recognises the
  replacement as its own service (no second bot — the trap the labels exist to prevent), and the
  recreate reuses `:latest`, which *is* the self-deployed build, so no rollback — true
  unconditionally only since issue #39: `:latest` is now applied by `takeOver()` *after* the
  replacement verifies, never at build time, so a build that merely compiled but crashed at boot
  can no longer leave `:latest` pointing at it. Don't "fix" the config-hash mismatch itself by
  computing a fresh hash — a wrong guess would make compose orphan the service and start a second
  container, the far worse failure.
- **The standby attaches nothing** until it has taken over: both instances hold the same
  `DISCORD_TOKEN` and Discord delivers every event to **both** gateway sessions, so a standby that
  registered handlers would double every command reply for the length of the overlap.
- **The handoff marker is its own file, not `state.json`.** One writer (the replacement), one
  reader (the original). `decideHandoffOutcome` reads it *before* the container state because a
  replacement that signalled `ready` is already stopping the original, so it can be caught mid-stop
  and would otherwise be misread as one that died.
- **`bot-ops.sh env-set` must never gain `--build`.** It recreates without building so it reuses
  the `<project>-bot:latest` tag a self-update just wrote; adding `--build` would rebuild from
  whatever the box's checkout is on, silently rolling the bot back on every settings edit.
- **`bot-ops.sh env-set` diffs before it validates a value's format, and format-validates only
  what changes; `env-get` and that diff read `.env` with compose's `env_file` semantics (issue
  #44).** Whitelist *membership* is still checked for every submitted key regardless of whether it
  changes — that's authority, not format. Don't "tighten" the format check back to running over
  every submitted line: a stored value the bot accepts but a whitelist regex rejects (a
  hand-quoted value, a CRLF-saved file, `1, 2` in `ADMIN_USER_IDS`) then fails every save that
  echoes it back, naming a key nobody touched — and the admin panel used to echo every field. `load_env_values` reads the file the way compose does (checked against `docker compose
  config`): last occurrence wins, `export KEY=` and indented lines count, whitespace/CR and one
  layer of matching quotes are stripped — so the panel shows, and diffs against, the value the bot
  actually runs with (not modelled: inline `#` comments, `${VAR}` interpolation, spaces around `=`,
  a `KEY: value` separator, escapes inside double quotes — nothing `env-set` writes can produce
  them). The rewrite still preserves every unchanged line verbatim (a missing final newline is
  added); only an indented or `export`ed line for a key being changed comes back as plain `KEY=`.
  `ops/bot-ops.test.ts` pins all of it with the real script, and the panel side (`applyPending`
  posts only the changed keys, composed by `planApply` from `planEnvSave` and `planPluginsSave`) is
  pinned by `ops/admin/server.test.ts`, which lifts those functions from the page source and runs
  `applyPending` against a stubbed page.
- **The bot container *starts* as root but *runs* as `bun`** (`user: "0:0"` in
  `docker-compose.yml` + `entrypoint.sh`). The daemon socket is mode 0660 `root:docker` and the
  docker GID differs per host (115 on the box, 999 on a stock Debian) — naming one would be the
  operator-supplied config nazumods/wow#879 rules out — so the entrypoint reads the GID off the socket
  itself (`stat -c %g`), then `setpriv`s to `bun` carrying that one supplementary group. Hygiene,
  not a boundary: the socket mount is already root-equivalent either way. Under the image's own
  `USER bun` (no socket, older compose file) it execs the CMD untouched. State written by a
  pre-drop root-run deployment stays root-owned — a one-time `chown -R bun:bun` on the host's
  `data/`, not something the entrypoint redoes every start.
- **`HANDOFF_FROM` alone is not enough to arm standby (#46).** It's baked into the replacement's
  env at creation time and container env is immutable, so nothing ever clears it — a swapped-in
  container carries its now-dead original's id forever. Without `resolveBootMode()` confirming the
  named original still *exists* on the daemon, every later in-place restart of that container
  (`bot-ops.sh restart`, the ops panel's Restart, a host reboot, a daemon restart, a crash loop
  under `unless-stopped`) would re-arm standby against an id that's long gone — a benign detour in
  the ordinary case, but an outage in exactly the degraded conditions (slow gateway login, a
  hung/refusing daemon socket) where a plain boot would have recovered.
  - **The check is existence (a 404), deliberately not the original's live running state.** A
    running-state check was tried and reverted: a container that merely hasn't been (re)started
    yet post-reboot still exists (`docker inspect` still returns 200, `State.Running: false`), so
    keying on running-state instead of existence would race a host reboot mid-handoff — both
    containers restart under `unless-stopped` in no guaranteed order, and if the replacement's own
    check landed in the window before the daemon had gotten around to restarting the original, it
    would misread a genuinely-in-flight handoff as "original gone," go live immediately, and never
    retire it — two live bots answering the same Discord token. Existence doesn't have that
    window: a container's object persists across a reboot regardless of whether it's currently
    running.
  - **A stopped-but-not-removed original stays `standby`.** `retireOriginal` removes the original
    only best-effort; on a failed removal the corpse also still holds the canonical name, so the
    replacement's own rename right after fails alongside it (name conflict, caught non-fatally) —
    the replacement is left visibly un-renamed, a state `retireOriginal`'s own comments already
    document as non-fatal-but-not-where-`bot-ops.sh`-looks. `resolveBootMode` re-arming standby on
    that container's later restarts is a narrower version of the pre-#46 bug (needs an independent
    `removeContainer` failure to trigger at all, versus every restart unconditionally before), and
    it's retried, not stuck: the next `takeOver` re-runs the same `retireOriginal`, whose removal
    may well succeed this time. That retry's own `stopContainer` call is handled, not left to
    propagate — see the `retireOriginal` gotcha below for how it tells a genuine first attempt
    (original still alive, so a stop failure must reach the original to reclaim) from a call
    against an original with nothing left to reclaim it.
  - **An unconfirmed daemon error — including a timeout — stays `standby`, never downgrades.** A
    genuine handoff boot needs a live daemon connection for the rest of the protocol regardless, so
    treating "can't tell" as "gone" risks two live bots on the shared token, same reasoning as the
    running-state case above. `docker.ts` bounds every exported call in its own `bounded()`
    wrapper, spanning the request AND the response-body read (`DEFAULT_TIMEOUT_MS`, 60s — #130;
    deliberately *not* at the `api()` funnel, which takes whatever signal it is handed, so the
    bound can cover a body read that happens after the headers land); the one daemon call
    `resolveBootMode` makes keeps a
    *tighter* 10s bound (`RESOLVE_BOOT_MODE_TIMEOUT_MS`) because it's the first daemon dependency
    `index.ts` has *before* `client.login()`, so a hung socket there falls through to "can't
    confirm" without eating 60s of boot before a login is even attempted.
- **`ready` is a promise of a stop, not the stop itself.** The replacement writes the marker
  *before* retiring the original, so a `retireOriginal` that dies in between would leave the
  original quiesced forever — seen `ready`, stopped counting, waiting to be killed. Two guards
  close that: the original bounds the wait for its own death (`RETIREMENT_DEADLINE_MS`, outcome
  `stalled` — reclaim, remove the replacement, report "check the daemon socket"), and past the
  `stopContainer` point of no return `retireOriginal` degrades instead of throwing, because the
  original is dead by then and a rejecting `ClientReady` would strand *both* containers.
- **`retireOriginal` can run against an original with nothing left alive to reclaim it — only a
  genuinely *live* original gets the strict `stopContainer` failure handling.** Two separate paths
  land here with the original already gone in some sense: (1) if a prior call's `removeContainer`
  failed, the original is left a stopped-but-not-removed corpse that still *exists*, and a later
  standby boot (raw env `bootMode`, or #46's daemon-confirmed `resolveBootMode`, whichever is
  current) retries `takeOver` -> `retireOriginal` against that same id; (2) `HANDOFF_FROM` never
  clears from a swapped-in container's own env (it's baked in at creation, and container env is
  immutable), so any boot path that still honors it against an id since fully removed — not just a
  corpse, gone entirely — lands here too. `stopContainer` already tolerates the expected 304
  (already stopped) / 404 (already gone), so the only way either call fails is a genuine daemon
  error — and on a genuine first attempt that must still propagate (the original is alive,
  watching, and reclaims at its deadline), but in both of the above there is no live original left
  to reclaim, so propagating would just crash the sole live bot via `takeOver`'s catch ->
  `exit(1)`, an `unless-stopped`-recoverable but wholly self-inflicted restart. `retireOriginal`
  tells them apart by inspecting the original *before* stopping it: running means a genuine first
  attempt (strict — let a stop failure propagate); anything else — not running, or gone entirely
  (`tryInspectContainer` returns `undefined`) — means nothing is left to reclaim (degrade a stop
  failure the same as every later step). An earlier version of this fix only covered the
  not-running case and missed `undefined`, which is in practice the *more* commonly reached of the
  two — caught in review before merging.
- **The standby is created with `RestartPolicy: {Name:"no"}`, never the original's — #160.** The
  original's real policy (`self.HostConfig.RestartPolicy?.Name || "unless-stopped"`) rides along
  instead, in the env (`HANDOFF_RESTART_POLICY_ENV`, stripped-then-reset exactly like
  `HANDOFF_FROM_ENV` so it can't stack across a chain of standbys). **`takeOver` restores it onto
  itself AFTER `tagLatest` and BEFORE `retire(originalId)` — that order is the whole fix, not an
  implementation detail.** #130 bounds `stopContainer` at 60s, so a stop the daemon genuinely
  performed but answered slowly is now indistinguishable, from the replacement's side, from one
  that never happened — if the restore ran only *after* that stop, the window between "original
  stopped" and "policy restored" would have BOTH containers down with neither's restart policy
  able to bring either back: a permanent outage, no race required. Restoring first means a throw
  at the stop is caught before the original is touched, and the original — still alive — reclaims
  normally. Never `{Name:"on-failure", MaximumRetryCount:0}` as an alternative to `"no"` — Docker
  reads `0` as *unlimited* retries, not zero; a trap, not a middle ground. `restorePolicy` is
  deliberately **not** best-effort like `tagLatest` — a swallowed failure would leave the
  *surviving* bot on `RestartPolicy:"no"` forever, silently, discovered only when a host reboot
  doesn't bring it back weeks later. Its own `exit(1)` (via `takeOver`'s outer catch) is **not**
  the `unless-stopped`-recoverable case the gotcha above describes for a *retire* failure — ON A
  FIRST ATTEMPT this container's own policy is still `"no"` when this failure fires (it's the very
  thing that failed to get restored), so recovery there is the ORIGINAL's job: still live, still
  polling (this is the only path that reaches this exit with the original genuinely alive — see the
  ⚠️ design-constraint section on #160), it reclaims via its own `RETIREMENT_DEADLINE_MS`/
  failed-marker path exactly as if the replacement had never signalled at all. A RE-ENTRANT
  `takeOver` (the "nothing left to reclaim" gotcha above — a stale `HANDOFF_FROM` re-arming standby
  on a later restart of a container that already retired successfully once) is different: this
  container's policy was already restored to the ORIGINAL's real value the first time around, so
  it is *not* still `"no"` here, and `unless-stopped` (or whatever that real value was) recovers it
  regardless of whether the stale `originalId` still names anything live. Don't read "recovery is
  the original's job" as universal — it's the first-attempt case; the retry case recovers by the
  policy already being right from before.
- **A stop that *throws* during `retireOriginal`'s live branch is no longer automatically a failed
  stop — #160.** Before the standby's own policy could be `"no"`, any `stopContainer` failure on a
  live original was unambiguous: propagate, let the still-alive original reclaim. Post-#160, #130's
  60s bound means a stop the daemon actually performed but answered slowly looks identical to one
  that never ran — and treating it as a failure would exit the replacement (now the only bot that
  can go live) over what was in fact success. The fix re-inspects on any throw: original confirmed
  gone or not running → the stop worked, log and proceed to remove/rename exactly as the ordinary
  path would; original still confirmed running → the stop genuinely failed, rethrow, unchanged.
  Only the live branch gets this — the already-stopped/already-gone branches already tolerated a
  stop failure outright (see the gotcha above) and don't need a re-inspect to tell why.
- **`redeploy()`'s pre-removal generation check compares the replacement's `State.StartedAt`
  against when its outcome was decided, not just whether it's currently running — #160, defence in
  depth.** Change 2 above already stops Docker itself from resurrecting an unverified standby, so
  this covers what's left: a manual `docker start` on a removed-but-still-present replacement, a
  standby a pre-#160 original created (still on `unless-stopped`), or `restorePolicy`'s own
  transport-drop mirror case (see `takeOver`'s docstring) genuinely restarting the replacement mid
  retirement-wait. `Date.parse(current.State.StartedAt) > observedAt` means a *different life* of
  the same container id — force-removing it would kill a bot that may be genuinely up and serving. A
  tie (same instant) is not `>` and is removed, matching the ordinary case exactly (nothing races
  its own `StartedAt` against itself). **`observedAt` is NOT always `awaitHandoff`'s own timestamp**
  — that's true only for `failed`/`timeout` decided directly from a poll. When `ready` is later
  demoted to `stalled` or `failed` (the retirement wait, up to `RETIREMENT_DEADLINE_MS` — 3 real
  minutes — after "ready" first arrived), `redeploy()` re-samples `observedAt` at THAT decision
  instead: an unrefreshed baseline pinned to the stale "ready" moment would wrongly read a
  replacement that restarted anywhere in that 3-minute window as "a later generation," silently
  skipping a removal it should make (found in review; `redeploy.test.ts`'s dedicated "between
  'ready' and the LATE stalled decision" test is the mutation guard). A confirmation inspect that
  itself fails must NOT skip the removal — that would be a new, silent way to leak a replacement
  container on an ordinary daemon blip; only a *positive* confirmed later generation ever skips it.
- **A `SIGTERM`/`SIGINT` handler now drains in-flight work before exiting — #154, the first signal
  handler in this codebase.** `beginHandoff()` doesn't itself await a tick already running (its own
  docstring says so) — the drain is `shutdown.ts`'s job, triggered by the `SIGTERM` a `docker stop`
  sends (an operator's, a compose restart, or #160's own `retireOriginal`). **One exception (#217):**
  a plugin tick that overruns `PLUGIN_TICK_TIMEOUT_MS` is abandoned by `pluginTicks`, so its tick's
  critical section closes while the call is still running, and neither a restart request nor this
  drain waits for it (`restart.ts`'s header). **The re-entrancy
  problem this solves: a straight "wait for `critical === 0`" deadlocks the auto-update path**,
  where `redeploy()` runs *inside* the very tick's `withCritical` section (`announce.ts`'s
  `onTick` → `guardedTick` → `withCritical` → `runTick` → `autoUpdate` → `applyUpdate` →
  `redeploy`) that would be waiting on itself. **The fix is a live exemption, not a frozen
  baseline** (`restart.ts`'s `handoffExemption()`): while a handoff is active, exactly 1 unit of
  `critical` depth is exempted — by construction the handoff's own holder (the tick, or a
  `/update`/`/plugins update` interaction, `commands.ts:124`/`:252`), which structurally can never
  close on its own while the handoff is in progress (`redeploy()` never returns on a successful
  handoff — the replacement kills this process mid-await). `awaitCriticalIdle` waits for `critical`
  to fall back to (or below) that live exemption. No handoff at all (a plain `docker stop`,
  `bot-ops.sh restart`, compose down) means exemption 0, i.e. wait for full idle. **An earlier
  version of this fix snapshotted `critical` at `beginHandoff()` time instead** (a "baseline") —
  found in review to be wrong whenever a genuinely unrelated critical section (a concurrently-open
  scheduler tick) happened to also be open at that instant: the snapshot baked its depth in too, so
  the drain reported "idle" immediately even though that tick's own write was still in flight,
  exactly the bug #154 exists to fix. A live "exempt exactly the one holder" has no such window,
  whatever else opens or closes and in whatever order.
- **The drain cannot delay a handoff past `RETIREMENT_DEADLINE_MS` (3 minutes) — the numbers don't
  allow it.** `SHUTDOWN_GRACE_MS` is 8 seconds total — the drain AND `destroyClient` together, see
  the next bullet — chosen to leave real margin under `stopContainer`'s own `t=10` (`docker.ts`) and
  compose's 10s SIGKILL default — **not** raised to make more room, since that's #160's territory
  (lengthens every handoff) not this one's. 8s is a small fraction of 180s (22.5×, a bit over one
  order of magnitude): even a drain that runs out its full grace before the daemon SIGKILLs the
  process is well under the deadline the *retirement* wait (`awaitRetirementFailure`) tolerates.
- **`shutdown.ts`'s drain and its `destroyClient` call are NESTED inside one `SHUTDOWN_GRACE_MS`
  budget, not sequential.** `destroyClient` is bounded to `min(DESTROY_CLIENT_TIMEOUT_MS,
  remaining time in graceMs after the drain)` — found in review that an earlier version gave
  `destroyClient` a fresh `DESTROY_CLIENT_TIMEOUT_MS` (2s) *on top of* the full drain budget (8s),
  an additive worst case of 10s that exactly tied (rather than beat) the 10s SIGKILL bound.
- **#184 added a THIRD nested phase — `disposePlugins`, between the drain and `destroyClient` —
  and each phase's remaining-budget calculation must measure elapsed time from the SIGNAL, not
  from whichever phase happened to run immediately before it.** `destroyClient`'s bound uses
  `Date.now() - startedAt` (total elapsed since the signal), not the drain's own `elapsedMs` —
  using the latter would let `destroyClient` believe it has more of the budget left than it
  actually does whenever `disposePlugins` itself took real time. A test asserting only the
  three-phases-together total (`shutdown.test.ts`) didn't catch this on its own — the per-plugin
  and per-phase timeouts already kept the total small enough either way — so a SECOND, narrower
  test (a `disposePlugins` fake that resolves ON ITS OWN partway through its bound, not via the
  bound itself) was needed to isolate it. `plugins/host.ts`'s own `disposePlugins(loaded, log,
  timeoutMs)` adds a FOURTH, per-plugin layer inside that: each plugin's own `dispose()` gets
  `PLUGIN_DISPOSE_TIMEOUT_MS`, so one wedged plugin can't block the rest — **critically, this
  requires disposing every running plugin CONCURRENTLY (`Promise.allSettled`), not one after
  another.** A sequential first version (found in review) let N plugins' timeouts sum — at N=2 that
  already exceeds `DISPOSE_PLUGINS_TIMEOUT_MS`'s own 3s outer bound, so the outer bound could fire
  and the process could exit mid-way through a LATER plugin's dispose, having never given it a real
  chance to run. Concurrent dispose makes N plugins cost the same worst case as one, so the outer
  bound stays a fixed, small backstop regardless of how many plugins are installed — not a value
  that would need to grow with the plugin count.
- **`writeJsonAtomic`'s temp name is per-process-unique (`${path}.${pid}.${counter}.tmp`) since
  #154, not the earlier fixed `${path}.tmp`.** The concrete hazard: the replacement's boot-time
  `loadPluginIndex` cache write (`index.ts`, before `takeOver` — so before the "the standby writes
  nothing until it has taken over" invariant `state.json` relies on even applies) and the
  original's still-running `pluginUpdates` tick re-fetching the same manifest are two DIFFERENT
  PROCESSES that can both write `data/plugins/index.json` during a handoff — a fixed temp name let
  either process's rename fail with `ENOENT` out from under the other. `src/plugins/index.ts` keeps
  its own deliberate copy of the atomic-write pattern (see that file's header — dependency-light on
  purpose) rather than importing `storage.ts`, so it needed the identical fix applied twice, by
  hand; the two must be kept in sync. In-process races are unaffected — `createJsonWriter`/
  `createKeyedJsonMutator` still own that, unchanged.
- **A second `/update` mid-swap answers `busy`** — the guard sits at the top of
  `checkForUpdate`, before any state write or network call, because interaction handling does
  not quiesce during a handoff (only the scheduler does) and `/update`'s `force: true` bypasses
  the anti-loop suppression: unguarded, it would overwrite the in-flight attempt's
  `pendingUpdateReport` and then force-remove its replacement.
- **The guard is two flags, not one (#51 item 4).** `handoffActive()` alone doesn't read true
  until `redeploy()` calls `beginHandoff()` just before creating the replacement (#130 moved it
  past the build) — so a second call landing in the stretch before that (both GitHub fetches,
  `saveState()`, `redeployAvailable()`'s daemon ping, and now the build, all async) used to pass
  straight through. `checkInFlight`, a module-level flag in `update.ts` claimed synchronously
  before the first `await` and released in `finally`, closes that window:
  two calls fired back-to-back (no `await` between them) can never both observe it as false.
- **`/report`** (`src/report.ts`) is disabled unless BOTH `REPORT_ROLE_ID` and `GITHUB_TOKEN`
  are set (it replies "not configured" otherwise). `project` is a fixed choices list, so
  `handleReportCommand` only ever builds a modal `customId` (`report:<project>`) for a real
  project — but the submit handler can't lean on that alone (a forged `modal_submit` interaction
  can carry any `customId`), so `handleReportModal` looks the project up via
  `config.ts`'s `repoForProject`, which uses `Object.hasOwn` rather than a bracket lookup: on a
  plain object, `REPORT_PROJECTS[project]` resolves inherited `Object.prototype` keys too, so
  `"constructor"`/`"__proto__"`/`"toString"` returned truthy and reached `createIssue` before this
  fix (#55, #186). With the fence closed, an unknown project genuinely can't reach the handler —
  it's refused at `if (!repo)` instead. `ensureLabel` treats HTTP 422 (label already exists) as success,
  so `/report` never fails on a missing `automated` label — it creates it on first use. Role check
  reads `member.roles` from the interaction payload (cached manager **or** raw `string[]`), so no
  privileged Members intent is needed.
- **A `/report` outcome is public; its refusals are not (nazumods/wow#870).** The modal submit defers
  **without** `MessageFlags.Ephemeral`, so the filed-issue confirmation (and the failure that
  replaces it) lands in the channel the report came from — the transparency is the feature, and the
  confirmation *is* the announcement, so there's no second message and no channel config. The three
  pre-flight refusals stay ephemeral on purpose: an unconfigured bot, a missing role, and an unknown
  project are the reporter's own business, not something the channel needs. Two consequences of
  going public, and both apply to **either** outcome the edit can carry — the success confirmation
  and the failure message that replaces it: the send passes `allowedMentions: { parse: [] }`,
  because the description is now untrusted free text in a public message and an `@everyone` typed
  into the modal would otherwise fire; and the content is clamped before it reaches `editReply` —
  `reportAnnouncement` clamps the success case to 2000 chars (the modal's Description field is
  unbounded), and the failure case runs the thrown `Error.message` through `clampReply` (`github.ts`),
  since it can embed an upstream body already bounded by `clampUpstreamBody` at the GitHub call site
  but the defensive clamp catches anything else that ends up in `err.message` too (#55, #186).
- `config.ts` reads env at import time (the `config` singleton) — tests/scripts must set
  `DISCORD_TOKEN` and `ANNOUNCE_CHANNEL_ID` **before** importing any module that imports it
  (see `config.test.ts`: env vars + dynamic import). Config *logic* is testable without env
  games via the pure `resolveConfig(env)`.
- **The WoW schedule/dedup gotchas moved to `@rackbops/plugin-wow` with the code (#107).** The
  Darkmoon Faire's realm-local "first Sunday" dedup keying (key on the *requested* calendar month,
  never `window.start`'s UTC parts, or the Faire silently never announces — issue #36),
  `dmfWindow`'s normalized-month return (else a year-boundary window keys twice and double-announces),
  the weekly-reset 10-min detection window, and the realm-transition silent-seed all live in the
  plugin now — preserved verbatim; see that repo's own tests.
- `COMMAND_PREFIX` (empty by default) namespaces every slash-command name so a second
  debug/staging bot can share a server without command collisions (`r_` → `/r_report`). It must be
  lowercase (Discord rejects uppercase names); `resolveConfig` validates and throws otherwise.
  A second bot is its own Discord application/token with its own state volume — and since
  `index.ts`'s `rest.put` fully replaces an application's command set, switching the prefix and
  restarting removes the old names automatically.
- **Build every command with `cmd(name)`, never a bare `new SlashCommandBuilder().setName("foo")`.**
  A hand-written name registers outside the namespace, and dispatch then has to cope with it:
  `bareName()` only strips the prefix when the name actually starts with it. An earlier
  unconditional `slice(prefix.length)` turned an unprefixed `update` into `date`, matching no
  case — the command appeared in Discord and silently did nothing. `cmd()` makes that
  unrepresentable; `bareName()`'s tolerance is the backstop.
- **A clean exit is not an update.** `restart: unless-stopped` respawns the *same image*, so
  self-update only does something if the respawn supplies rebuilt code (manual `--build`, or a
  registry image + Watchtower-style updater). The `attemptedUpdateToSha` marker exists precisely
  because the naive version exit-loops forever against a non-cooperating orchestrator: once the
  bot has exited for a sha and come back unchanged, it warns instead of exiting again.
- **Staleness is ancestry (containment), not sha equality (nazumods/wow#871).** `GIT_SHA` is `git rev-parse HEAD`
  — the tip the image was built from. `decideUpdate` asks whether that build *contains* the newest
  `BOT_BRANCH` commit, via a `ShaRelation` from `fetchShaRelation()`
  (`GET /compare/{latest}...{running}`): `identical`/`ahead` → `current`, `behind`/`diverged` →
  `restart`, fetched only when the shas differ so the common path still costs one request. Post-fork
  there is no path filter (the whole repo is the bot — see the Fork-gotchas bullet), so a deploy is
  `identical` right after a build and `behind` once newer code lands. (nazumods/wow#871 originally adopted
  ancestry for the monorepo era, where the whole-repo HEAD was usually *ahead* of the last
  bot-subdirectory commit and bare equality wrongly flagged a correct deploy stale — a divergence
  that no longer arises now that every commit is a bot commit.)
- **`BOT_BRANCH` is queried through the GitHub API, so it must exist on the remote.** A deploy
  running a local-only branch (e.g. an unpushed integration branch that merges several PRs) can't
  point at it. The *running sha* being unpushed is handled, though: the compare 404s, which is its
  own `ShaRelation` (`unpublished`) and resolves to `disabled` naming the sha — so such a deploy
  can keep `GIT_SHA` baked, where it previously had to be built without one. `unknown` (any other
  compare failure) deliberately falls back to the pre-nazumods/wow#871 "mismatch = stale", so a GitHub outage
  degrades the check rather than failing startup.
- **`reportUpdateOutcome()` clears and saves `pendingUpdateReport` *before* it tries to deliver.**
  Delivery is the part that can fail — an expired token, closed DMs, a deleted channel — and a
  report left in place after a failed send would re-fire on every subsequent boot. Losing one
  follow-up beats wedging the marker. For the same reason `index.ts` calls it **after**
  `startScheduler` and doesn't await it: an owed follow-up must never delay or crash startup.
- **The interaction follow-up posts unauthenticated via raw `REST.post(Routes.webhook(...), { auth: false })`,
  not `WebhookClient`** — the webhook route is authenticated by the interaction token itself, and
  discord.js's `WebhookClient.send` typing can't set the ephemeral flag, which the follow-up needs
  to match the ephemeral `/update` reply it continues. **Measured on the box's debug bot (nazumods/wow#681):
  the token does survive the restart** — the follow-up lands ephemerally in the original command's
  thread, so DM and channel are true fallbacks rather than the load-bearing path.
- `requestRestart()` exits **immediately** when no critical section is open — anything that must
  survive the exit (a Discord reply, a state write) has to run inside `withCritical()`. The
  `/update` handler wraps its `checkForUpdate` for exactly this reason; without it the process
  dies before `editReply` lands.
- Run `bun run check` (tsc) and `bun test` after changes. `.github/workflows/ci.yml` now runs
  both on every pull request (`on: pull_request` only — a direct push to `main` runs no CI)
  (`bun install --frozen-lockfile`, `bun run check` for both projects, one
  root `bun test`) — the fork's own CI, not carried over from the original monorepo's
  path-scoped `discord-bot-test.yml` (see **Fork gotchas**). No lint beyond tsc.
- **`cloudflared` now has something to route to**: the `warbandeer` plugin's ingest endpoint
  (`@rackbops/plugin-warbandeer`), once `WARBANDEER_INGEST_PORT` is set. The sidecar itself is still
  opt-in (`--profile tunnel`) and unchanged in shape — it joins the bot's Compose network, so
  the server is reachable at `http://bot:<WARBANDEER_INGEST_PORT>` once a public hostname is
  mapped to it in the Cloudflare dashboard. The bot process itself never reads
  `CLOUDFLARE_TUNNEL_TOKEN` — only the sidecar container does.
- Every `*.test.ts` that reaches the `config` singleton (directly or transitively — `update.ts`
  and `commands.ts` both do) must prime `DISCORD_TOKEN`/`ANNOUNCE_CHANNEL_ID` and use a dynamic
  `await import()`, or it throws when run **standalone**. A full-suite `bun test` can mask this:
  `config.test.ts` sorts first and primes the env for everyone. Check with `bun test src/<f>.test.ts`.
- **Intents come from the Plugin Index JSON, resolved before `createClient()` — never from
  plugin code (#98).** discord.js freezes `IntentsBitField` at `new Client()`
  (`node_modules/discord.js/src/client/Client.js` — `new IntentsBitField(options.intents).freeze()`),
  and nothing plugin-side may run before `takeOver()` completes a handoff anyway (the standby
  invariant above), so an intent a plugin needs has to be declared in its Plugin Index entry
  (`PluginIndexEntry.intents`, numeric `GatewayIntentBits` values so the JSON needs no discord.js)
  and unioned in by `collectIntents()` — there is no path for a plugin to request an intent at
  `activate()` time and have it take effect that boot. `src/plugins/*` (index.ts, registry.ts)
  stays free of `src/config.ts` on purpose — it's read-only data plumbing that has to work before
  config exists in the boot sequence.
- **A Plugin Index entry describes the index's CURRENT version only — never read it as a fact
  about the version a bot is pinned to (#222).** `PluginRelease` carries no host API, intents, env
  keys or commands, so `PluginIndexEntry.hostApiVersion` can rule out `entry.version` and nothing
  else. Before #222 `selectPlugins` skipped a plugin whenever that differed from the bot's, so a
  plugin pinned to a working 1.x was disabled at the next restart the moment the index published a
  2.0.0 declaring a different host API. Now an OLDER version is kept when the current one needs a
  NEWER host, provided every version `installPlugins` may try (the explicit `PLUGINS=name@version` pin
  and only that; else #104's `targetVersion` and, if that install fails, the last-good
  `installedVersion`) is provably older than `entry.version`, **and the entry declares no intents** —
  they would be unioned into the whole `Client` for a plugin that only ever runs the older version,
  and a privileged one the operator hasn't enabled is an unrecoverable login failure. Still skipped,
  as before: no pin; a version equal to, newer than or unorderable against the current one; a
  current needing an OLDER host than the bot's (assuming a plugin's host API never decreases across
  its versions, no older version can match); an entry with intents;
  and a plugin with no pin and no `state.json` record — `installPlugins`' newest-cached fallback is
  not modelled (it needs disk access). **#225: a plugin left out of `PLUGINS` no longer loses its
  record** — `buildPluginStateFile` carries its previous entry forward as `enabled: false` (see the
  gotcha below), so the no-record case above is now only a fresh install or a deleted `state.json`.
  Limits: the pins carry no provenance, so a rolled-back bot (state written under a newer host API)
  or a skipped explicit pin that `buildPluginStateFile` recorded as `installedVersion`
  (`host.ts`) can keep a version this bot never ran; **#223: `install.ts`'s `reconcileManifest` now
  checks a kept version's own declared `hostApiVersion` — against this host's `HOST_API_VERSION`, on
  both the cache-reuse and post-extract paths — and refuses a mismatch instead of loading it** (a
  divergence in the bundle's `commands`/`env` only warns; a missing/unparseable `package.json` never
  refuses); and other current-entry data still applies
  to an older running version — the env keys `createHostApi` hands over, and the command names the
  pre-load collision check reads (a kept plugin claims its CURRENT entry's names against the plugins
  after it, so a later plugin sharing one is skipped, with the reason, even if the older version
  registers no such command). `intents: []` says the current entry declares none, not that the older
  version needs none — one that relied on an intent the newer version dropped runs without it — and
  a plugin that declares any intent, even `Guilds`, keeps the old failure. Only a per-release schema
  (option 2 of #222) would fix those. `requests.ts`'s pre-flight also rules out only the
  current version, but accepts any other (no direction, ordering or intents test), so an update
  request it accepts can still be skipped at the next boot. `contract.ts`'s doc on
  `HOST_API_VERSION` ("skipped … never loaded") is now imprecise for the same reason; it is left as
  is because the plugins repo vendors that file verbatim behind a `check-contract` drift check.
- **Extraction is atomic, and the installed bundle's own manifest is reconciled against the host on
  every load (#224, #223).** `install.ts`'s `tryInstallVersion` extracts into a sibling `.staging-*`
  dir under `<dataDir>/plugins/<name>/`, then renames it into `<name>/<version>/` only after
  `reconcileManifest` passes — never straight into the final `versionDir` — so a `tar` that dies
  part-way (OOM kill, container stop, disk full) leaves nothing at `<version>/dist/plugin.js` for the
  cache-reuse fast path to pick up unverified next boot; any earlier partial `versionDir` is cleared
  first, and `newestCachedVersion`'s directory listing excludes a leading-`.` name so a crashed
  extract's staging leftover is never mistaken for a cached version. `reconcileManifest` reads the
  extracted `package.json`'s `botPlugin` block — otherwise dead weight, since everything else the host
  acts on (env, commands, intents) comes from the Plugin Index, not this file — and is called on BOTH
  that fast path and a fresh extract, since a pinned bundle is served by the fast path forever and
  never re-extracted. It refuses only a `hostApiVersion` divergence from the HOST's own
  `HOST_API_VERSION` (NOT `entry.hostApiVersion`, which describes only the index's current version —
  comparing against it would wrongly refuse a #222 `keepOlder` bundle whose own manifest still says
  the OLDER host API it was actually built against); `commands`/`env` divergence only warns, and a
  missing/unparseable `package.json` (an already-installed version cached before this change, or a
  test fixture) never refuses — refusing there would brick every already-installed plugin on the next
  boot. The function is total (never throws): its cache-path call sits outside `tryInstallVersion`'s
  own `try`, so a throw there would take `installPlugins` down for every plugin, not just this one.
- **Off plugins stay in `state.json` (#225).** Taking a plugin out of `PLUGINS=` used to drop its
  entire `state.json` entry on the next boot — `installedVersion`, `skippedVersion`, `remindAt`,
  `scheduled`, all gone. Re-enabling it later then fell through to `newestCachedVersion`, which can
  silently reinstall a version the operator moved *away* from (install 1.2, decide it's bad, pin back
  to 1.1 — both version directories now exist on disk; disable and re-enable and the bot comes back on
  1.2). `buildPluginStateFile` now carries the previous entry forward as `enabled: false, active:
  false` for any plugin not selected this boot, keeping `installedVersion` (the pin — the whole
  point), `notifiedVersion`/`skippedVersion`/`remindAt`/`scheduled`, and `configured`/`missingEnv` as
  the last boot it ran left them; it drops `targetVersion`/`availableVersion`/`error`, which only a
  boot that actually ran the plugin can know. Nothing acts on an off entry: `decidePluginUpdates` and
  `runDueSchedules` (`updates.ts`) both skip it — no notification, no reminder, and a scheduled update
  is **paused, not fired**, resuming at the first update tick after the plugin is turned back on (two
  restarts close together; `/plugins list` and the panel show the pending schedule throughout, so this
  is documented behaviour, not a hidden trap). `requests.ts`'s `validate` and `/plugins update` both
  refuse `update-now`/`schedule` for an off plugin (`<name> is off — turn it on first` / turned-off
  wording); `remind`/`skip`/`cancel` still apply since they're bookkeeping only. A `pendingReport`
  whose plugin was turned off in the same restart that consumed its update pin reports "is turned off,
  so the update … did not run" rather than a false success or a confusing revert message. **Not fixed
  here:** the admin panel still offers *Update now* on a carried-off plugin's card until #244 folds in
  an `enabled` check — the bot refuses the request with the reason above, which the panel surfaces, so
  nothing silently succeeds, but the button itself is misleading until then.
- **A plugin option that asks for autocomplete gets a warning, not a working picker (#218).** Nothing
  in `src/` routes an autocomplete interaction to plugin code — `buildCommandBody` (`host.ts`) still
  registers the command (dropping it would remove a working command over a dead picker; a typed value
  still works), but warns once per command, naming every option path (`autocompleteOptionPaths`, which
  walks subcommand groups/subcommands recursively) that declared `autocomplete: true`. `index.ts`'s
  `InteractionCreate` handler answers every autocomplete interaction with `interaction.respond([])`,
  so Discord's picker shows "no options" immediately instead of failing after its own 3s timeout with
  no explanation anywhere in the log. Actually routing an autocomplete interaction to a plugin's own
  handler needs an addition to `PluginCommand` in `contract.ts` — out of scope here (that file is
  vendored verbatim by `rackbops-bot-plugins`) — tracked as its own contract-change issue, #287, to
  land with the post-#236 contract batch (#219/#248/#220) in one paired vendor bump.
- **`ops/admin/Dockerfile`'s `COPY server.ts admin-contract.ts ./` must name every relative local
  import `server.ts` has, or the admin container crash-loops at boot.** #124 added the
  `./admin-contract` import; a missed COPY update shipped in the same epic and crash-looped with
  `Cannot find module './admin-contract'` (#128) — nothing but the real image build caught it,
  since unit tests and source-based render checks run against source, never the built image. Now
  guarded mechanically: `server.test.ts`'s "Dockerfile COPY ratchet" `describe` (#163) parses both
  files and fails, naming the missing filename, if a local import isn't COPYed — a future import
  added without updating the COPY line is caught here, not at boot.
- **A URL-path allowlist for the admin-asset routes must reject `%`-encoding outright, not just a
  literal `..`.** `resolvePluginProxyUrl` (`ops/admin/server.ts`) rejects any `%` in its untrusted
  `path` input before checking literal `.`/`..` segments, because a percent-encoded `..` (`%2e%2e`)
  sneaks past a literal-segment check and is then normalized by the URL parser inside `fetch`,
  escaping the package prefix — even reaching jsDelivr's `/gh/` endpoint (caught by #124's review
  gate, a security-lens finding). `resolveAdminBundleUrl`'s own untrusted input
  (`installedVersion`) doesn't need the same pre-check: it validates against a whole-string
  anchored semver regex (`INSTALLED_VERSION_RE`), which already excludes `%` by construction rather
  than by an explicit rejection. Both functions re-assert the resolved (WHATWG-normalized) URL
  still starts with the plugin's own package prefix after `new URL()`, as defense in depth against
  any other normalization escape the pre-checks miss.
- **The admin tab (bundle + `proxyFetch` assets) follows the plugin's INSTALLED version; `getEnv`
  scope / `env-set` validation / `HostApi.env` still follow the index's CURRENT entry (#165).**
  `ops/admin/server.ts`'s `resolveAdminBundleUrl`/`resolvePluginProxyUrl` derive a
  `cdn.jsdelivr.net/npm/<package>@<installedVersion>/...` URL from an `?v=` query param (the client
  supplies it from `/api/plugins`' `installedVersion`, never a server-side `bot-ops status` lookup
  per fetch) so the tab configures the code that's actually running, not the manifest's current
  release — before #165, `debug` served the 1.1.0 tab over the running 1.0.0 warbandeer plugin. A
  malformed `?v=` is a 400, never a silent fallback to the manifest's version. `getEnv`/`setEnv` are a
  **deliberate, documented exception**: they still read the manifest's `env` keys (`host.ts:57`,
  `bot-ops.sh load_plugin_keys`, `server.ts:743`) regardless of which version's tab is mounted —
  per-version env-key scoping would need the bot to cache each installed version's manifest entry
  and is not built. A plugin's admin tab therefore configures the running code's UI, but a config
  key that changed shape between versions is validated against the CURRENT manifest, not the
  installed one.
- **`ops/bot-ops.sh` on a deployed instance can silently drift behind the panel image it's paired
  with, and the panel now stamps + checks a schema for it (#173).** `install.sh` fetches
  `bot-ops.sh` once and nothing re-fetches it after a later merge changes it — a real incident:
  `debug`'s admin panel was rebuilt from `main` after #121 added the `plugin-request` subcommand,
  but its deployed `bin/bot-ops.sh` was still the pre-#121 copy, so `Update now` failed with a bare
  `bot-ops: usage: bot-ops.sh {status|logs [N]|restart|env-get|env-set}` and nothing named the real
  cause. `bot-ops.sh` gained `readonly BOT_OPS_SCHEMA=<n>` (bumped in the same PR as any subcommand
  or `ALLOWED_SPEC` change) and a `version` subcommand printing `{"schema": <n>}`;
  `ops/admin/server.ts`'s `REQUIRED_BOT_OPS_SCHEMA` is a hand-mirror, drift-pinned by a test that
  regexes the script (the `DEFAULT_PLUGIN_INDEX_URL`/`HOST_API_VERSION` pattern). The panel runs
  `version` once at startup (`checkBotOpsSchemaStartup`), logging either an info line or a loud
  `OUT OF DATE` one — a pre-#173 script's usage error on an unrecognized `version` subcommand
  counts as outdated too, same as a real number mismatch (`decideBotOpsSchema`). Never refuses to
  start on a mismatch: `status`/`logs`/`restart` still work against an outdated script, this only
  makes the drift visible instead of silent — `/api/status` gains `botOpsOutdated: true` (present
  only when true — `mergeStatusOutdated`) and the panel renders it as a banner at the top of the
  page. `install.sh`'s own summary line for `bot-ops.sh` prints the schema it just installed, read
  back from the file it just wrote, so the two numbers are easy to compare after a deploy.
  **`version` is dispatched before ANY instance-config precondition — round-3 review caught the
  first cut getting this backwards.** The original design ran `version` through `main()` like every
  other subcommand, which requires `BOT_OPS_PROJECT`/`CONTAINER`/`CONFIG_DIR`/`COMPOSE_FILE` and a
  real `.env`/compose file to exist first — so a genuinely CURRENT script pointed at a bad instance
  config (an unset or wrong `BOT_OPS_CONFIG_DIR`, a missing `.env`) failed `version` with an
  instance-config error, and the panel reported the SAME `OUT OF DATE — re-run ops/install.sh` line
  as a real pre-#173 script, sending an operator to fix the wrong thing. `version` needs only `jq`
  — no docker, no config dir, no compose file — so it (and its `need jq`/`die` helpers) are now
  defined and dispatched right after `readonly BOT_OPS_SCHEMA=1`, before `main()`'s preconditions
  even run. This is what actually decouples "is this script current" from "is this instance
  configured right": only a genuine schema mismatch, or a pre-stamp script's usage error (which has
  no early `version` check to hit at all, so it still falls through to its own preconditions and
  then its `*)` usage fallback), is ever reported as out of date now.
- **`bot-ops.sh`'s env whitelist is ONE ordered spec (`ALLOWED_SPEC`, an indexed array of
  `"KEY|regex"` entries), not two hand-maintained structures — #133 collapsed what used to be a
  `declare -A ALLOWED` (membership + format) plus a separate `ALLOWED_ORDER` indexed array (display
  order), which existed only because a bash associative array does not preserve insertion order.
  The two drifted in practice, so `cmd_env_get` used to carry a ~15-line runtime assertion
  (`"... have drifted"`) that existed purely to catch that drift. `ALLOWED` (the `${ALLOWED[$key]}`
  lookup every validation site expects) and `ENV_KEY_ORDER` (env-get's display order — named to
  match `PLUGIN_KEY_ORDER`'s own convention, not the old `ALLOWED_ORDER`) are now both DERIVED from
  `ALLOWED_SPEC` once, by `build_allowed_from_spec`, at load — so the two literally cannot disagree
  now, and the drift guard was deleted rather than kept as dead insurance. `REQUIRED` stayed a
  separate, hand-maintained set on purpose: "has no default to clear back to" is a genuinely
  independent fact about a key, not something `ALLOWED_SPEC`'s shape could carry for free — its own
  `REQUIRED`-vs-`ALLOWED` check in `cmd_env_set` was kept (a typo there would silently never enforce
  a key). `env-get`'s output shape (a flat `{KEY: value}` object) is unchanged — this was an
  internal refactor only. Carved out of the same issue, needing a design decision first: emitting
  each key's `pattern`/`required` to the panel so `ops/admin/public/index.html`'s hard-coded
  `REQUIRED_KEYS`/`BRANCH_NAME_RE` mirrors could go away — a contract change across
  `bot-ops.sh`/`server.ts`/the panel, landed by #205 (the `env-schema` subcommand and
  `GET /api/env-schema`) and #207 (the panel fetches it alongside `GET /api/env` on load, drives
  required-marking/blank-check/format-check/the `BOT_BRANCH` chooser filter from it, and drops both
  hardcoded mirrors and their drift tests). Degrades gracefully: an old deployed `bot-ops.sh`
  (pre-#205) or a transient `GET /api/env-schema` failure leaves the panel with no client-side
  validation at all — `env-set` stays the authority either way, same as the #173 schema-gate banner
  already covers for a missing subcommand.
- **A plugin's own `secret: true` env key is WRITE-ONLY through `bot-ops.sh` (#240, ADR-0006
  decision 8) — its VALUE never leaves the script.** `env-set` accepts one; `env-get`
  never emits the key (let alone its value); `env-schema` lists it with `secret: true` and a boolean
  `isSet`, never the value; `env-set`'s result, every `die` message this script writes, stderr and
  `docker.log` (argv) are value-free too — the text it relays but does not write, `docker compose`'s
  output (`env-set`'s `log`, and everything `restart` prints), is scrubbed on a best-effort basis (item 5
  below). What holds that together: (1) secret keys live in their own
  `PLUGIN_SECRET_ORDER`/`PLUGIN_SECRET_FORMAT`/`PLUGIN_SECRET_REQUIRED`, deliberately NOT in
  `PLUGIN_KEY_ORDER`, so nothing that lists keys can reach one; (2) **`RESERVED_KEYS`** — the
  deployment's and the core's own keys, in three groups: core credentials and access control, every
  `${VAR}` `docker-compose.yml` interpolates, and (#278) the settings the bot core reads that the panel
  does not edit (`GITHUB_REPO`, `PLUGIN_REGISTRY_URL`, `BOT_DATA_DIR`, `NODE_ENV`; three the redeploy
  machinery or docker sets but the core reads from the environment: `HANDOFF_FROM`,
  `HANDOFF_RESTART_POLICY`, `HOSTNAME`; and four the discord.js `Client` the core builds reads:
  `SHARDS`, `SHARD_COUNT`, `SHARDING_MANAGER`, `SHARDING_MANAGER_MODE`) — are dropped from a manifest
  whether it declares them secret or not, so a manifest can't make `DISCORD_TOKEN`, or the bot's data
  directory, editable or listable, with or without its plugin enabled. Pinned by tests: against
  `.env.example` (every credential-shaped key; and every documented key is editable, reserved, or one of a
  named list of plugin-owned settings), the compose file, and a regex scan of the core's source (each
  variable it reads through `env.X`, `env["X"]`, `required`/`optional`/`list("X")` or a `*_ENV` constant is
  editable or reserved — a net, not a proof: it misses destructuring and dynamic names, and what a
  dependency reads, which is why the four shard variables are pinned by the behaviour table instead), so a
  new core key fails a test until it is decided. Runtime-level variables (`NODE_OPTIONS`, `PATH`, the
  proxy variables, `TAR_OPTIONS`) are read by the runtime or a spawned tool, not by the core's code, and
  stay claimable (#240's item 22; #280 tracks reserving the proxy variables and `TAR_OPTIONS`). (A
  credential a first-party plugin owns is
  deliberately NOT
  reserved: the wow plugin's `BLIZZARD_CLIENT_ID`/`BLIZZARD_CLIENT_SECRET` are declared `secret: true`
  in its Plugin Index entry and nothing in `src/` reads them, so the panel can set them — the test pin
  names them in an explicit exemption list and checks each sits under `.env.example`'s "Used by the
  <plugin> plugin" block); (3) **fail closed on what a manifest says** — a key that ANY plugin in the
  index declares secret is never listed as a plain key, whichever plugin declares it plain (and, since
  #256, a secret key of a usable entry is editable, write-only, whether or not its plugin is in
  `PLUGINS`); a secret declared twice is first-wins; a
  `secret` that is not exactly `false`/`null`/absent (`"yes"`, `1`, `[]`) counts as secret; and an entry whose
  `key`/`format`/`required` holds a CR or LF (or that has no string `format`) is unusable, because the
  row reader takes five raw lines per key and a stray newline would re-frame every later row and let a
  hostile entry forge a plain row for another plugin's secret key — but an unusable entry that claims
  `secret` still marks its key secret (never editable), so it cannot be forgotten into a listing; (4) **env-set's own decision reveals nothing about a
  stored secret** — a submitted secret is always treated as a change and written, because a "no
  changes" answer would tell a caller its guess equals the stored value (the one skip is a blank for
  an already-unset key, which `isSet` already reveals); (5) **what compose prints is relayed through
  `relay_tool_output`** — `env-set`'s `log` and everything `restart` prints — since compose quotes a
  `.env` line it refuses to parse. Two layers. **First, a message that is ABOUT the env file is withheld
  whole**: one that names `$ENV_FILE` or says "env file" is replaced by this script's own sentence — it
  says that compose's output mentioned an env file and is withheld because such a message can quote the
  file's contents, names the line numbers compose gave (if any), and says the original is what the same
  command prints on the host. It names no path and claims no cause or remedy, and it is the same whether
  the command failed or succeeded (the relay never sees the exit status, and compose also says "env
  file" about the stack's own `.env`, a different file). The premise, sourced: compose-go's dotenv reader
  wraps every parse error as `failed to read <path>: …` (compose-spec/compose-go `main`,
  `dotenv/format.go`, read on 2026-09-21 — upstream `main`, NOT the compose installed on any host, which
  was never run for this), so the messages most likely to quote the file name it; withholding does not
  depend on guessing WHICH part of a line compose printed, which cannot be made exact (review round 5:
  compose ends a key at `=` OR `:`, drops `export `, and counts U+0085 / U+00A0 as whitespace, so a guess
  made with bash's rules missed wherever the two disagree). **Second, anything else is scrubbed**
  (`redact_secret_values`, because any other message can quote a value too), best
  effort, of what `env-get` would not print. What that is comes from `.env` and the static `ALLOWED`
  table ALONE, never from the Plugin Index: the
  index is unavailable exactly when the bot is down, which is when compose complains, and a scrub that
  leaned on it scrubbed nothing then (review round 4). So a core credential, a plugin's secret, a
  plugin's plain setting and a key nobody knows are all scrubbed; only a static `ALLOWED` key written
  the plain way is left. A `KEY=value` line gives its value; any other line — `KEY: value`,
  `KEY = value`, `export MY!KEY=…`, the tail of a pasted multi-line secret — gives the whole line, the
  line without `export`, and what follows its first `=` and its first `:`; each text is trimmed of
  compose's whitespace and tried with and without its quotes. Every definition of a key counts, not
  only the last; the values `env-set` read
  before it rewrote the file count too (a replaced value is no longer in it; the new ones already are);
  and values are replaced LONGEST FIRST — a short value
  replaced first cuts a longer one that contains it in two, which then no longer matches, so whoever
  could set one secret could unmask another. A text under six characters is left alone (scrubbing
  `us` or a port out of compose's message would make it unreadable) — which assumes nobody stores a
  credential that short; one that is, is not scrubbed. `restart` captures compose's output to do this,
  so by hand it appears when compose has finished rather than as it goes (the panel never showed it
  earlier, and shows none of it after a timeout). Known limit, pre-existing: a secret written
  `KEY: value` is invisible to `load_env_values`, so `env-get` calls the key unset and `env-set` appends
  a `KEY=` line beside it instead of replacing it. Best effort, not a proof: a tool that prints a value
  transformed is not caught by the second layer, and a compose that
  echoed caller-controlled text into `log` could still act as a guess oracle for a stored secret (not
  seen, and not testable without a real compose); (6) a value
  holding a CR is refused (message names the key only) before its format regex runs, since a
  permissive secret pattern must not let one start a new `.env` line, and no key's value may contain
  `$` or a quote ANYWHERE, static or plugin, secret or plain (compose reads a `.env` value as syntax:
  `SPOTIFY_CLIENT_ID=${DISCORD_TOKEN}` would interpolate a core secret into a plugin's key,
  `PLUGIN_INDEX_URL=https://host/?t=${DISCORD_TOKEN}` would send it to that host, and a quote can open a
  value that swallows the lines after it; a manifest `format` such as the shipped `^\S+$` and the static
  `PLUGIN_INDEX_URL` regex admit both. Not just a LEADING quote, because compose trims a wider set of
  whitespace than bash's `[[:space:]]` — U+0085 and U+00A0 among it — before looking for one. `main`
  checked neither; no shipped key needs either character. Only a value that would CHANGE is judged: a
  plain key resubmitted with the value it already holds is skipped first, so a stored value that
  holds one never blocks an unrelated save, while a submitted secret always counts as a change and is
  always judged); (7) a refusal names a submitted
  key only when it looks like a variable name (`echo_key`: upper-case, at most 40 characters), since a
  multi-line value is read line by line and a later line's text before its `=` would otherwise be
  echoed. Existing `env-get` output and every non-secret `env-schema` entry keep their shape, and their
  bytes for every key the script listed before, with four deliberate differences: (i) a key in
  `RESERVED_KEYS` that a manifest used to make listable is gone from both and no longer editable; (ii) a
  plain key that ANOTHER index plugin declares `secret` — whichever of the two is in `PLUGINS` — is gone
  from `env-get`, but is in `env-schema` as a secret row (`secret: true`, `isSet`, the secret
  declaration's pattern) and is editable, write-only; (iii) since #256 the keys of a plugin that is NOT in
  `PLUGINS` are listed and editable too, because the loader reads every plugin in the cached index (the
  panel learns which plugin owns a key from `/api/plugins`, and the cache and the panel's own fetch of the
  index can disagree for up to the bot's ~15-minute index refresh: a plugin the panel already lists may
  have no keys here yet); (iv) which declaration of a key governs it is the FIRST in index order, on or
  off — so a plugin that is off and listed before an enabled one that declares the same key decides that
  key's format and required-ness (the shipped index has no key declared by two plugins). (`RESERVED_KEYS`
  also covers the settings the core reads that the panel does not edit, since #278: an index entry that
  names `GITHUB_REPO` or `BOT_DATA_DIR` is dropped, on or off.) A panel admin who can set `PLUGIN_INDEX_URL` controls the index this all reads
  from — and the plugin code the bot then installs — so write-only is a property against the panel,
  its logs and its screens, not against whoever owns the index.
  The same posture covers `plugin-request`: a `webhook-add` URL travels on stdin only, is matched in
  bash (never `jq --arg`, never argv), is never echoed by a `die` (`echo_safe` shows a short
  printable field or `(not shown)`; the rest name the field, not the value), and its request file is
  written under `umask 077`; the `routing-set` snowflake check uses `\A…\z` because jq's `$` also
  matches before a trailing newline. `routing-get` reads only `routing.json` and `discovery.json`
  (the bot's webhook store is never named or opened) and scrubs on the way out — best effort, since
  neither file is meant to hold a URL: it drops members named exactly `url`/`token`/`secret`/`password`
  and redacts webhook-URL-shaped strings, and does NOT catch a bare token under another name, an id and
  token split across fields, or a percent-encoded URL (`webhooks` and `inviteUrl` are legitimate
  members of these files, so broader name matching would delete real data; the same drop would hide a
  plugin literally named `url`/`token`/`secret`/`password` from the `plugins` map). **Treat any new path
  that could print a secret value as a blocker** — `everythingObservable` in `ops/bot-ops.test.ts`
  is the check that walks every output channel for the secret's value.
- **`docker-compose.yml` on a deployed instance drifts the SAME way `bot-ops.sh` does, and #178
  extends #173's mechanism to cover it rather than inventing a second one.** `install.sh` fetches
  the compose file once too and nothing refreshes it — a real incident: `debug`'s stack compose was
  still the 2026-09-05 copy even after both bot and admin images were rebuilt from `main`, so #168's
  `environment: BOT_ENV_FILE` and #140's `cloudflared` image pin were both merged but silently NOT
  in effect. `docker-compose.yml` gained a top-level `x-rackbops-schema: <n>` key (Compose ignores
  any `x-` key, verified by `ops/docker-compose.test.ts`'s real `docker compose config` test class
  — this must never break that validation); `cmd_version` reads it straight from
  `$BOT_OPS_COMPOSE_FILE` via a `grep '^x-rackbops-schema:' | cut` one-liner, reported as
  `composeSchema` in `version`'s JSON — `null` (never an error) when that var is unset, the file
  doesn't exist, or the key/value is absent or malformed, so `version` stays precondition-free
  (#173 round 3) for compose too. `ops/admin/server.ts`'s `REQUIRED_COMPOSE_SCHEMA` is a second
  hand-mirror, drift-pinned the same way. `checkBotOpsSchemaStartup` now decides BOTH files
  independently (`decideBotOpsSchema` / `decideComposeSchema` — deliberately two separate pure
  functions, never one parameterized by field name, so a refactor can't accidentally conflate the
  two files' drift into each other) and logs one line per file via a shared `logSchemaLine` helper;
  `/api/status` gains `outdatedFiles: string[]` (some subset of `["bot-ops.sh",
  "docker-compose.yml"]`, present only when non-empty) alongside the existing `botOpsOutdated: true`
  convention, and the panel's banner text is composed dynamically (`describeOutdatedBanner`,
  lifted from `index.html` and pinned in `server.test.ts`) to name precisely which file(s) are
  behind rather than a static sentence naming only `bot-ops.sh`. `install.sh`'s summary line for
  `docker-compose.yml` prints the schema it just installed, read back from the file it just wrote,
  the same pattern as its `bot-ops.sh` line.
- **A plugin with an entry in `routing.json` and no servers lives nowhere; a plugin with no entry
  lives in the home server. The two are different on purpose (#237, ADR-0006).** `isPlaced` is true
  for `{ servers: {} }`: the operator placed the plugin, and placed it nowhere. Reading an empty
  `servers` as "unplaced" would turn "take this plugin out of every server" into "put it back in the
  home server". So getting a plugin back to unplaced means removing its entry, not emptying it, and
  `validatePluginRouting` accepts `{ servers: {} }` deliberately. `pluginsForGuild`'s tests pin the
  difference (`a plugin placed nowhere lives nowhere`).
- **In routed mode the bot owns the command list of every server it is in (#239).** Registering a
  server is a PUT, and Discord's PUT replaces that server's whole list. So a server no plugin lives in
  gets the core commands and nothing else, and a command someone registered there by hand is removed
  the first time routing places anything. A deployment that already has commands live in a second
  server must seed `routing.json` to mirror them before its first routed boot (#247). A
  `routing.json` that places nobody changes nothing: the bot keeps registering as it always did — to
  `DISCORD_SERVER_ID`, or globally when that is unset — and touches no other server. Any entry that
  survives `readRouting`'s repair counts as placing, even one for a plugin that is not loaded
  (`hasPlacements` counts the keys the repaired file has). Registration re-runs, in full, whenever
  something asks for it: a `routing-set` from the panel (#241) and a server the bot joins (#259) both call
  `applyRouting`, so a server the bot is invited to after boot has its commands at once (the core ones, and
  those of any plugin already placed there: a kicked and re-invited server keeps its placement) and appears
  in `discovery.json` within seconds, with the outcome of that registration -- bar the gaps in the next
  gotcha.
- **A join decides its mode OUTSIDE the chain, and the availability events are not joins (#259).**
  `guildJoined` reads `routing.json` itself (`hasPlacements`) and only then calls `applyRouting` or
  `refreshDiscovery`; both queue themselves on live.ts's one chain, so choosing the mode from inside a
  queued job would wait on itself forever. Between that read and `applyRouting`'s own the last placement
  may be removed, which turns the run into a single-mode one, and single mode rethrows a failed
  registration; `guildJoined` catches it, because it is called as `void guildJoined(guild)` from an event
  listener, where a rejection would be a process-level event. discord.js 14.27.0 emits `guildCreate` only
  for a server that is new to its cache once the client is Ready (`client.guilds.cache.get(data.id)` misses
  and `client.ws.status === Status.Ready`, in `src/client/websocket/handlers/GUILD_CREATE.js`), so the
  servers listed at start-up (each is added to the cache by `READY` first) and a server returning from an
  outage (`guildAvailable`) never reach it, and `guildDelete` only when the guild really goes (an outage
  returns early as `guildUnavailable`, in `src/client/actions/GuildDelete.js`). `index.test.ts` pins that
  the two availability events are not wired, and a discord.js bump that changes those handlers is what to
  re-read. Three gaps follow from wiring only these two events, and are accepted. A server invited while
  the bot's gateway session is being re-identified (not resumed) arrives as `guildAvailable`, not
  `guildCreate`: `READY` puts a stub of every server it lists, new ones included, in the cache before its
  `GUILD_CREATE` arrives, which then takes the "guild is cached" branch. Such a server gets no join, so its
  commands wait for the next registration and its place in `discovery.json` for the 15-minute tick. A
  kick while the bot is disconnected emits no `guildDelete` at all (the cache keeps what a later `READY`
  no longer lists), so the server stays in the cache, and in discovery, until a restart. And a server
  invited during an outage, whose first `GUILD_CREATE` says it is unavailable, reaches `guildCreate` as a
  stub with no name and no channels: the join registers against the stub, the full data arrives later as
  `guildAvailable`, and nothing refreshes discovery until the tick. (A resume is understood to replay what
  was missed, as `guildCreate`, so it is not affected; that is the gateway protocol, not something this
  repo ran against a live gateway.) Wiring `guildAvailable` was not built: it fires for every server
  coming back from an outage, and a join is not that; a version that filtered it to servers not seen
  before would close the first and third gaps, and nobody has asked for it yet.
- **A damaged `routing.json` is repaired silently in memory, and the log says what the repair left out —
  once (#260).** `repairRouting` is tolerant and pure: a malformed plugin, server or webhook entry is
  dropped, so a plugin placed in a server whose entry is wrong lives nowhere and nothing on the wire
  shows it, and #247 seeds this file by hand. `readRouting` therefore also logs `droppedByRepair(raw)`,
  a second pure function beside repair that names each dropped plugin, server or channel by its KEY
  (through `shown`, which cuts anything over 40 characters down to 37 and `...`, so a hostile key is
  clipped) and, for a bad `postTo`, its clipped value; nothing else a person typed is quoted. Each is a
  line `[routing] routing.json: …; it is ignored` on `console.warn`, and the same is said when the file, or
  its `plugins` or `webhooks`, is not an object at all. `readRouting` runs for every plugin command used in
  a server, every announcement and every join, so each distinct message is said once per process
  (`resetRoutingWarningsForTest` is the seam): a problem that is fixed and then put back is not said again
  until a restart, and a file with thousands of bad entries says thousands of lines, once. Not said: a
  webhook entry with a stray `url` key (repair keeps the entry and drops the key, so nothing was ignored,
  and the URL must never reach a log), a malformed `results` entry (the bot's own bookkeeping), and
  anything a panel write (`mutateRouting`) repairs — that path is silent and writes the repaired file
  back. The report is log output only: what the bot registers, posts or refuses is decided by
  `repairRouting(raw)` exactly as before (a logger that throws is swallowed, and so is a report that throws,
  said once in its place, because `gateCommand` fails open when a read throws), and `model.test.ts` runs a table of fixtures through both functions so the
  report and the repair cannot drift. In the same spirit `applyRouting` warns, once per distinct message,
  when routed mode has a plugin with commands that nobody placed and `DISCORD_SERVER_ID` names a server the
  bot is not in, since those commands are then registered nowhere.
- **`announce` rejects only when EVERY target failed (#243).** A plugin that sees `announce` reject
  typically posts again on its next tick, and a retry posts again to the channels that DID get the
  message — once a minute, for as long as one channel stays unreachable. So a partial failure is logged
  (`[announce] <plugin>: could not post to <channel>`, with the error) and resolves, and only when
  nothing was posted does the first error propagate, unchanged (the others are logged, so none is lost).
  With one target — no routing — that is exactly what `announceTo` did before. The catch: a plugin cannot
  tell that one of its channels never got a message; the log line is the only trace.
- **A webhook post that fails ambiguously can be posted twice, and webhook posts are serial (#243).** A
  timeout or a 5xx does not say whether Discord kept the message, and the plan chose that any webhook
  failure falls back to the bot, so a message that was in fact delivered can appear twice in that channel:
  a message that must arrive is worth more here than one that arrives exactly once. The targets are also
  posted one after another, each webhook allowed ten seconds, so a plugin that posts to several channels
  whose webhooks all hang can spend `10 s × N` (before any bot fallback) against
  `PLUGIN_TICK_TIMEOUT_MS`, 30 s (#217: the wait on that tick is abandoned, and the tick is not started
  again until the call settles); a webhook that hangs without answering "gone" is never marked broken, so
  it does this on every post. Concurrent
  posting, a shorter timeout or an overall deadline are the ways out if it is ever seen.
- **Only 401/404 marks a webhook broken, and the flag lives in `routing.json`, not `discovery.json`
  (#243).** "Gone" is what Discord says for a webhook that was deleted or whose token no longer works;
  a rate limit, a 5xx, a timeout or a message over Discord's length limit is a bad moment, and marking a
  working webhook broken would stop the bot using it until someone added it again. The flag is
  `webhooks[channelId].broken` (the reason), written by `markWebhookBroken` through `mutateRouting`
  without touching `updatedAt` or `updatedBy` — nobody edited anything — and only on the entry that was
  posted through: the same `id` and `addedAt` as when the post was made (`withWebhookBroken`). A post can be
  in flight for ten seconds, and the 404 of a webhook the operator has just replaced must not mark its
  successor broken. The issue text said `discovery.json`; #237's model already keeps it beside the rest of
  the webhook's metadata.
- **A webhook post carries `allowed_mentions: { parse: [] }` to match the Client default (#243).** The
  Client sends everything with `allowedMentions: { parse: [] }` (#48), so a plugin message containing
  `@everyone` pings nobody when the bot posts it. A webhook does not inherit that: without the field the
  same message would ping through the webhook and not through the bot. It is a security property, not a
  nicety, and `sends content with allowed_mentions parse []` compares the whole body.
- **The webhook URL is never in a log line, an error or `routing.json` (#243).** It leaves
  `routing.secrets.json` only to be handed to `executeWebhook`. Every reason is a fixed string (plus a
  status number), and the error a failed fetch threw is dropped: Bun's carries the whole URL in its
  `path` property (`{ code: "ConnectionRefused", path: "https://discord.com/api/webhooks/<id>/<token>" }`),
  which a logger prints.
  There is no redaction step because no string in `post.ts` is ever built from the URL and other text; the
  token-search test drives every failure path and looks for a distinctive token.
- **A thread's own id is on no channel list, and a channel that cannot be looked at is not refused
  (#243).** `interaction.channel` is `null` for a thread that is not cached, so `whereOf` fetches it
  once; if the fetch fails (an uncached thread whose fetch fails, or a channel the bot cannot view), or a
  thread reports no parent, the parent is *unknown* and the gate lets the command run instead of refusing
  it for a channel it could not look at, logging a `[gate]` line. That is the price of failing open: a
  command typed in such a channel runs even where it would be refused.
- **The admin panel's stylesheets are a generated theme plus a tokens-only file, served from an
  exact-match map — three things to know before touching either (#238).** (1) `public/rb-theme.css`
  is generated from `@rackbops/styles`, pinned **exactly** in `ops/admin/theme/package.json`: to bump
  it, edit the pin, `bun install` and `bun run build` in `ops/admin/theme/`, and commit the
  regenerated CSS with `package.json` and `bun.lock` — never hand-edit it. `server.test.ts`'s "is
  stamped with the version pinned…" compares the file's first line with the banner `composeThemeCss`
  writes for the pin, so a bump without a rebuild (or a hand-edit of the banner) fails; it needs no
  `node_modules`, and CI installs none for the theme package (only the root and `ops/admin`), so no
  test may import `@rackbops/styles`. (2) The page's `<link rel="stylesheet">`s are answered by
  `handleRequest` from `HandlerConfig.assets`, a `Map` keyed by the exact request path and built at
  startup by `loadStaticAssets` from `STATIC_ASSET_FILES` — the request path is only ever a key,
  never a filesystem path, so `/%72b-theme.css`, `/ADMIN.CSS`, `/admin.css/` and `/constructor` all
  404. A new stylesheet the page links **must be added to `STATIC_ASSET_FILES`**, or it 404s in
  production; "every stylesheet the page links is served" fails until it is. The image needs no
  Dockerfile change for one (`COPY public ./public` already ships everything in `public/`).
  `loadStaticAssets` is tested against the real files; the `import.meta.main` block only calls it and
  puts the result in the `HandlerConfig` (a source-pin test checks both), and a stylesheet that can't
  be read makes the boot throw rather than 404 on a live page. (3) `admin.css` uses `--rb-*` tokens
  only, so a re-theme can't strand a literal (pinned by "admin.css uses tokens only": no hex value,
  colour function or CSS named colour anywhere in a value — a `var()` fallback included — colour
  properties accept tokens and a few keywords only, `font-family` and radius are tokens, and every
  `var()` names a token the theme defines). The page carries no `<style>` block and none of the old
  custom properties (`--bg`, `--muted`, …); "every class the page uses is styled" fails on a class
  that neither stylesheet defines, and "the controls carry the design-system classes" pins which
  classes each control gets. A plugin's settings tab is bare elements the panel cannot class, which is why `admin.css`
  styles them with selectors that sit wholly inside `:where(.plugin-admin-tab …)` — zero
  specificity, so a bundle's own rules win (a test pins that shape).
- **The Apply bar derives what is pending from the controls on every event and never stores it (#257).**
  The bar's only state is its phase (`idle` / `applying` / `done` / `failed`) and the message it is
  showing; the plan comes from `collectPending()` → `planApply` each time, exactly as the deleted Save
  buttons read the controls at click time. So Discard is "re-render from the baseline" (`loadPlugins()` +
  `loadEnv()`, nothing posted), and there is no second copy of the truth to drift. Two consequences for
  anyone touching the page. (1) **Anything that changes a control's value from code must dispatch an `input`
  event**, or the bar never sees it: setting `.value` fires none, which is why the chip editor writes its
  hidden carrier after it is built only through `syncTagValue` (`TAG_SYNC`), a source-pinned line. (2) **The
  collector reads exactly two selectors**, `#env-fields [data-key]` and
  `#plugins-list input[type=checkbox][data-plugin]`, and `#app`'s delegated `input` / `change` handler
  (`onControlEdited`) ignores an event from outside `#env-fields, #plugins-list`: a plugin's own settings
  bundle renders arbitrary DOM inside this page, so a page-wide `[data-key]` would be a bug, not a
  convenience (the test harness's `querySelectorAll` throws on any other selector).
  Also: `collectPending` passes `plugins: null` -- so no `PLUGINS` change is ever planned -- when the bot's
  state is unreadable (`stateError`, or no `/api/plugins` yet; every plugin box is then rendered disabled) or
  when no plugin box is rendered at all (a failed `/api/plugins` reload keeps the previous answer in memory
  but swaps the boxes for an error line, and "nothing ticked" read against it would post `PLUGINS=` and
  switch every plugin off). When bot-ops.sh answers `recreated:false` ("no changes"), the bar says nothing
  was restarted rather than claiming a restart. The bar's two text lines are written only when they change
  (a keystroke must not rewrite a live region), and `admin.css` pads focus scrolling
  (`scroll-padding-bottom`, more while a refused / failed `--danger` bar is up) so a Tab stop does not land
  under the sticky bar; the bar's text is height-bounded (`max-height: 30vh`) as two SEPARATE scrollers -- a
  long compose log scrolls inside the TITLE and never pushes the hint beneath it (the backup path) out of
  view, and a refusal that echoes a huge pasted value scrolls inside the hint. The bar is hidden with the
  `hidden` attribute while nothing is pending, which restates as `display: none !important` in `admin.css`;
  its `role="status"` text is inside it, so whether a screen reader announces the FIRST appearance depends
  on the reader (it is not verified here). While a request is in flight both buttons are disabled and focus
  rests on the bar itself (`tabindex="-1"`); it lands on OK when the request ends, and returns to the open
  tab (or the bar) when OK / Discard hide the focused button.
  **After a failed apply the controls are re-read from the bot unless the failure IS one of `env-set`'s own
  refusals (#272).** `failureWroteNothing(status, result, text)` (`APPLY_VIEW`) is true only for a 502 whose
  body is not JSON and has a line that starts `bot-ops: env-set: ` (`die()` prints `bot-ops: ` + its
  message, and every `die "env-set: ..."` in `cmd_env_set` sits before the write, `mv "$tmp" "$ENV_FILE"`):
  the controls keep the user's values and ticks, and the bar shows `Couldn't apply: …` with Discard and
  Apply. The page thereby relies on a message the script owns, so **that order is pinned** by `bot-ops.sh's
  env-set refusals all precede the write` in `server.test.ts` (it reads the script, as the schema drift pin
  does): the pin is TEXTUAL, so it fails when a code line after the write, or outside `cmd_env_set`, mentions
  `env-set:` (a `die`, an `echo`, a message in a variable, a continuation line, a heredoc), when `die()`
  stops printing the `bot-ops: ` prefix, or when `cmd_env_set` stops having exactly one write; it does not
  catch a behaviour change that builds the text from pieces, an `ERR` trap, or a second write of `.env`
  by other means. Everything else re-reads both lists, after which nothing is pending and the bar keeps
  only OK (for the length of the re-read the bar still shows what was pending: the same window as #275): a
  JSON 502 (the recreate failed after `.env` was rewritten, #47), a 504 (we killed it: the outcome is
  unknown), any other status, and the plain-text 502s the rule deliberately does not trust because they
  can follow the write -- a `set -e` abort after the `mv` (a tool's own error text), an external kill
  during the recreate (the `bot-ops: env file` line, printed only after the write, perhaps after a
  chown/chmod warning), a kill between the `mv` and that line (an empty body) and a proxy's own 502 (HTML);
  a refusal before the write that has no env-set prefix (a failed backup or `mktemp`, the self-update
  guard) also re-reads: the safe side. The `catch` (a network error, or the page's own timeout) leaves the
  controls as they are: a reload from a server that cannot be reached would replace the input with two
  error lines. Where the page never got an answer (that `catch`) but the write happened anyway, its edits
  are still there, and a retry finds the values in place, gets `recreated:false` and the bar says "Nothing
  needed applying." with the hint that the SAVED settings already held them, because after a killed
  recreate the running bot may not: nothing in the panel finishes that recreate (Restart is
  `docker compose restart`, which does not reload the env file; tracked in #277).
- **A request file that may carry a webhook URL is deleted on rejection, never moved to
  `requests/rejected/` (#241).** A webhook URL is a secret, and `rejected/` is a folder nobody treats
  as one and nothing ever prunes. The drain decides a file may carry one from its NAME (the writer
  names a file for its action, so `webhook-add`), from its TEXT (`webhooks/<id>/<token>`, with or
  without a host, so a port, a doubled slash, a trailing dot or no host at all does not hide it -- after
  every JSON unicode escape, backslash-slash and percent-escape is read, once, as the character it
  stands for; a doubly-encoded spelling is not chased) and from
  what it PARSES as. For such a file a rejection deletes it and its reason carries no parser text
  (`unreadable JSON` alone); an unparseable file that is not secret keeps the parser's message with its
  quoted text taken out (Bun quotes a bare identifier, which could be a token). An update request
  whose text holds a URL is refused and deleted outright: it never legitimately has one, and its
  `requestedBy` would be stored in `state.json`. A bare token with no URL around it cannot be told from
  any other text, so its file is set aside like any unparseable file -- what must not happen is a log
  line quoting it. If a delete fails the error names the file and nothing else. The file that carries
  an accepted URL sits in the mailbox for about one drain interval (five seconds) at the container's
  default mode before the bot deletes it: accepted, because shortening it belongs to the writer
  (`bot-ops.sh`); it is there longer if the bot cannot delete it (the log says so, the request is not
  applied again, and the delete is retried on every drain) or if the drain is busy ahead of it (a webhook
  lookup can take 10 s, and a half-written file costs one 250 ms wait). A bug in this area would show up as a token in a log line, a reason, a
  result, a temp file or `rejected/`: `secrets-leak.test.ts` searches all of them.
- **`routing.json`'s `results` is how the panel learns the outcome of a request (#241).** The bot
  appends `{ id, action, plugin?, channelId?, ok, reason?, at }` under the `id` the panel chose, keeps the
  newest 20, and the panel learns the outcome by reading the file, so it polls for its own id; a
  request with no valid `id` records nothing, and a refusal that failed to parse is still recorded under
  its id. An id names one request, so a result under an id that already has one replaces it (a request
  handled twice cannot push the other requests' outcomes out of the list). Update actions record nothing
  -- their feedback is `state.json`, as before. A failed result write is logged and swallowed: it never
  turns an applied request into a rejected one.
- **A routing request is "applied" once `routing.json` is written (#241).** `routing-set` writes the
  file, THEN calls `applyRouting` to re-register commands, then discovery is rewritten; a server that
  refuses its commands (Discord's 50001) is not a failed request and shows against that server in
  `discovery.json`, and an `applyRouting` that throws is logged and swallowed. `webhook-add` writes the
  secret first and the metadata second -- and if the second write fails, puts the previous secret back --
  so `routing.json` never names a webhook that has no URL, and the two files name different webhooks
  only if that rollback write also fails (which is logged, without the URL). `webhook-remove` writes in
  the reverse order and clears whichever of the two files holds
  the channel, so a webhook that was half removed can be removed again.
- **The panel's routing routes are server-native for the same reason `/api/plugins/request` is, plus one
  more: the SERVER mints the request id (#242).** The same reason: `requestedBy` comes from the verified
  session (`email:<email>` or `token`), never from the body. The extra one: `queueRoutingRequest` adds the
  `id` the page then looks up in `routing.results` (`HandlerConfig.newRequestId`, default
  `crypto.randomUUID()`), after the request's own fields, so a client cannot choose an id that would
  replace another request's recorded result (a result under an existing id replaces it, #241). Each body is
  REBUILT from validated fields (`parseRoutingSetInput` / `parseWebhookAddInput`; a `__proto__` server key
  is refused before it can be assigned), so an unknown key never reaches the mailbox. A webhook URL goes
  body -> parsed value -> the JSON payload on `bot-ops.sh`'s stdin and nowhere else: not `argv`, a log
  line, a response or an error message. A bad one (or one over 512 characters, which no real URL is) is a
  bare `400 bad webhook url`, a `runBotOps` that throws is caught (a fixed `502`; its message, redacted and
  clipped, is the log line), and whatever `bot-ops.sh` wrote (its stderr in a failure, its `queued` in a
  success) passes through `redactWebhookUrls` first: the
  panel's own copy of the bot's redactor with the decode step of the drain's detector in front (a JSON
  unicode escape, `\/` and a percent-escape are read, once, as the character they stand for), because the
  panel cannot import `src/`. Its snowflake, webhook-URL and plugin-name patterns are hand copies of the
  bot's, like the update-request ones, and a change to the bot's has to be made here too. The panel never
  calls Discord: there is no webhook preview route, the bot looks a webhook up when it applies a
  `webhook-add` and records the channel it landed on in `routing.results`. The four writes and
  `GET /api/routing` (`routing-get`, a plain `buildInvocation` branch) sit behind the same auth and
  cross-site checks as every `/api/` route; `server.test.ts` pins each of them per route rather than the
  routes carrying a second copy.
