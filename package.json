{
  "name": "rackbops-discord-bot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "description": "Discord bot: DMF/reset timers, server-up announcements, addon release notifications",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "check": "bunx tsc --noEmit"
  },
  "dependencies": {
    "discord.js": "^14.27.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.0"
  },
  "overrides": {
    "undici": "^7.29.0"
  }
}
